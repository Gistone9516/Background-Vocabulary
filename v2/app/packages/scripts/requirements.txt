# relation-index.py 가 쓰는 파서. gate 체인 안에 있어서 이것이 없으면 게이트가 통과하지 않는다.
#
#   pip3 install --user -r packages/scripts/requirements.txt
#
# 이 파일이 없던 동안 의존이 어디에도 선언돼 있지 않았다. 새 머신에서는 게이트가 스무 개 검사
# 중 relation-index 자리에서 ModuleNotFoundError 로 멈추는데, 무엇을 깔아야 하는지가 코드의
# import 문 말고는 아무 데도 적혀 있지 않았다.
#
# 버전을 아래로 내리면 안 되는 이유. tree-sitter 는 0.22 에서 API 가 바뀌었다. relation-index.py
# 는 Language 를 인자 하나로 부르고(31~33행) Parser 에 언어를 생성자로 넘긴다(75행). 0.21 이하
# 에서는 Language 가 인자를 둘 받고 언어는 set_language 로 넣어야 해서 그대로는 돌지 않는다.
#
# 아래 버전은 2026-08-15 Ubuntu 22.04, Python 3.10.12 에서 전 게이트를 통과시킨 조합이다.
# 올릴 때는 게이트를 한 번 돌려 보고 올린다.
tree_sitter==0.26.0
tree_sitter_typescript==0.23.2
tree_sitter_css==0.25.0
