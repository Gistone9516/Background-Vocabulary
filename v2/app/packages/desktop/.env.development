# dev의 API 주소. Tauri dev가 vite 서버(5185)를 로드하므로 같은 오리진이고, /api는
# vite 프록시를 지나 로컬 서버(8787)로 간다. 이것은 명시된 설정이지 폴백이 아니다(DS-3).
# 프로덕션 빌드는 이 파일을 읽지 않으며 VITE_API_BASE 미설정 시 vite.config.ts가 빌드를 깬다.
VITE_API_BASE=/api
