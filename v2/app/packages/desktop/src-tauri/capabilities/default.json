{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 권한 + C4 S2 로그인(oauth·opener) + S3 캐시(store). 퀵 캡처(S4)는 Rust 쪽 등록이라 JS 권한이 필요 없다(DS4-7 — 필요한 것만 연다). 앱 자체 커맨드(secret_*)는 capabilities 대상이 아니다.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "oauth:default",
    "opener:default",
    "store:default"
  ]
}
