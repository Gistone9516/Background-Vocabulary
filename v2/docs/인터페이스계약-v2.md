# 배경노트 v2 — 인터페이스 계약 (SoT v2.0, 2026-07-21 — 4렌즈 적대 검토 반영판)

> buildflow ②단계 SoT. **충돌 시 이 문서가 권위(SoT wins).** 패키지별 스펙은 여기에 conform.
> 상위 정본: 요구사항 = `SRS-요구사항명세.md`(v1.0) · 기술 결정 = `ADR-아키텍처결정.md`(v1.0) · v1 계약 = `v1/docs/배경어휘-사이드탭-인터페이스계약.md`(출발점, 본 문서가 대체).
> 표기: [승계] = v1 계약 그대로 유효, [개정] = v1에서 변경, [신규] = v2 신설.

## 0. 권위 규칙 [승계·개정]
- 화면 흐름·노출 필드·문구·태깅 UX = 프론트(ui-shared) 설계가 권위. 백엔드가 굽힌다.
- 로케일 라우팅·RAG 필수·고위험 거부·TTL·모델 라우팅·티어 판정·한도 = 백엔드가 권위. 프론트가 따른다.
- [개정] 영속 데이터(세션·어휘 자산·지식 상태·프로젝트)의 정본 = **서버 DB**(SRS Q-07). 클라이언트 저장소는 캐시일 뿐이며 충돌 시 서버가 이긴다.

## 0-1. ★ 이식성 불변 경계 [승계 — AWS가 "나중"에서 "지금"이 됐어도 경계는 동일]
- **core/·shared/·ui-shared/ 안 런타임 전역 0건.** AWS SDK·Tauri API·Node 전용 API·DOM 밖 브라우저 API 직접 호출 금지. 외부 자원은 전부 포트(`LlmClient·SearchProvider·CacheStore·UserRepository·SessionRepository·AssetRepository·KnowledgeRepository·ProjectRepository·UsageCounter·EnvConfig`) 뒤로 추상화하고 주입(DI). (검토 정정: §3-3의 4개 영속 엔티티 전부에 대응 포트 존재 — 목록이 완전하다.)
- **웹표준만 사용:** fetch · Request/Response/Headers · Web Streams · Web Crypto · TextEncoder/Decoder · URL · AbortController/AbortSignal.
- **런타임·공급자 특수성은 adapters/에만:** Lambda 부트(streamHandle)·Data API 클라이언트·Upstash REST·Tavily REST·Tauri invoke·로컬 Node 부트(@hono/node-server)·로컬 DB 드라이버.
- **하드플로어 가드 3중(NFR-505, ADR-004):** ① TS 프로젝트 레퍼런스(참조 없는 패키지는 컴파일 불가) ② portability-guard(정규식, v1 승계) ③ 경계 검사(순환·역참조·딥임포트). 셋 중 하나라도 위반 = 빌드 실패. [C1 정정] ③은 dependency-cruiser로 계획했으나 그 도구가 자기 소스에서 `#utl/*` self-subpath-import를 쓰고 Node(Windows)가 **한글+공백 경로에서 subpath-imports 해석을 실패**해 이 프로젝트 경로에선 실행 불가(버전·pnpm 설정 무관, 최소 재현으로 확정). 동일 계약(순환·역참조·배럴 경유)을 의존성 없는 `packages/scripts/boundary-check.mjs`로 대체 강제한다.

## 0-2. ★ 실행 3계층 [신규 — ADR-003 로컬 dev 모드]
| 계층 | 부트 | DB | 캐시·리밋 | LLM·검색 | 용도 |
|---|---|---|---|---|---|
| mock | 없음(클라 목) | 없음 | 없음 | 목 | UI 개발 |
| local | `adapters/local`: @hono/node-server | 로컬 PostgreSQL(Docker) **단일** | 인메모리 또는 Upstash | .env 실키 또는 목 | 통합 테스트 |
| aws | `adapters/aws`: Lambda Function URL(RESPONSE_STREAM) + streamHandle | Aurora Serverless(PG) + Data API | Upstash REST | Secrets 실키 | staging·prod |
- **같은 Hono 앱 인스턴스**를 세 부트가 공유한다. 앱 조립(라우트 등록)은 `adapters/http-app`(가칭)에 있고 부트만 계층별. 라우트 코드에 계층 분기 금지 — 분기는 주입된 포트 구현이 담당.
- SQL은 PostgreSQL 방언을 정본으로 작성. **SQLite 대안은 폐기한다**(검토 정정: SQLite JSONB는 PG와 바이너리 비호환, FK 기본 OFF 등 §6 스키마를 그대로 실행 불가 — "방언 흡수" 전제가 성립 안 함. 로컬은 Docker PG 단일). 통합 테스트 통과 기준은 PG.
- **mock 계층 등가성 게이트**(검토 반영): mock의 StreamEvent 스키마·순서는 shared의 이벤트 타입에서 파생하고, C3 게이트에 mock↔local 이벤트 대조 테스트(같은 시나리오의 이벤트 시퀀스 스키마 일치)를 포함한다 — 등가성을 규율이 아니라 게이트로 강제.

## 1. 화면 ↔ 엔드포인트 흐름맵 [개정 — v1 §1 + v2 신규]
| 화면 | 엔드포인트 | 프롬프트 | 비고 |
|---|---|---|---|
| landing(Astro) | — | — | 정적. 설득 콘텐츠(FR-954). 앱 진입 링크만 |
| entry(컴포저) | `POST /classify` | P1 | [승계] domain_risk 권위=백엔드. condition_required 포함 |
| narrow(아키네이터) | `POST /next` (루프) | P2 | [승계] enough·confidence, 엔벨로프 [3,8] |
| 난이도 선택 | `POST /preview` | P-preview | [승계 v1 후반] 깊이별 대표 어휘. 캐시(세션+답변 키) |
| 연결 턴 | `POST /relate` | P-relate | [승계 v1 후반] 프로젝트 kept 조건부 재인 질문 |
| terms(카드 스트림) | `POST /recommend` (SSE) | P3 | [승계] exclude 페이지네이션·2차 위험 게이트·취소 전파 |
| 카드 상세 | `POST /detail` | P5 | [승계] what/whymine/how 3단 + sources 필수. deepen 폐기 유지 |
| 기본 정리 | — (클라) | — | [개정] 구조화 포맷 템플릿(FR-604). 무비용·즉시 |
| AI로 더 정리 | `POST /summarize` | P4 | [승계] pro 전용. 구조화 출력으로 개정 |
| ★구조화 구체화 | `POST /refine-primer` (루프) | P6[신규] | [신규 FR-607] pro 전용. 별도 아키네이터(다지선다), 프라이머 정본 갱신 |
| 마인드맵(지도) | `GET /map` | — | [신규 FR-312] 서버가 자산·태그 그래프 반환. 일반/프로젝트 스코프 |
| 세션·자산·프로젝트 | `GET/PUT /sessions… /assets… /projects…` | — | [신규 — 서버 정본화] §5 |
| 인증 | `/auth/google` `/auth/refresh` `/auth/logout` | — | [승계 §8, 개정: PKCE 표준 OAuth redirect(웹), 데스크톱은 시스템 브라우저+루프백] |
| paywall | 게이팅 402/429 | — | [승계] free 주간 한도. 광고는 free 화면 정책(TR-09, 클라 소관) |
| refusal | (domain_risk=high) | — | [승계] 부드러운 거부, 문구 정본 = ui-shared 다국어 리소스 |
| deep link | — (클라) | — | [신규 FR-606] 프라이머 지참 링크. 화이트리스트·검증 §6-4 |

## 2. 공유 타입 (shared/types.ts) — v1 승계 + v2 델타

### 2-1. 파이프라인 타입 [승계]
`Choice`(label, domain_tags — **deeper 필드 없음**, SRS §6.1) · `Term`(필수 term/kind/group/priority/why/one_line/tag, 조건부 direction/use_example/context_note/relates_to/order/gap_type) · `Source{title,site,url}` · `Prompt1~5 In/Out` · `RecommendInput`(domain_risk, exclude) · `StreamEvent` · enum `JobType` 9종·`GapType` a~e·`Tag`(알아/몰라/적용모름) — 전부 v1 계약 §2 그대로. kind는 자유 문자열.
- **PreviewIn/Out·RelateIn/Out** [승계 — 정본은 v1 코드]: v1 계약 문서에는 없고 **v1 코드 `shared/types.ts`가 정본**이다(v1 후반 기능). C1 이식 시 그 정의를 그대로 shared로 옮기고 본 절에 복사 반영한다(이식 전 임의 재설계 금지).
- **★프롬프트 지시 델타 승계(검토 정정 — 타입 승계와 별개다):** v1 `shared/prompts/index.ts`의 확정 지시 내용 전부 승계 — P2의 enough·confidence 판정 지시, P3의 exclude 제외·group 부여·앵커 제외 지시, P5의 what/whymine/how 3단+sources 지시, preview·relate 빌더 포함. **C1 이식 게이트에 "프롬프트 빌더 파일 diff = v1 대비 의미 변경 0" 검사를 포함한다**(구조 게이트가 못 잡는 텍스트 소실 방지).

### 2-2. 인증·엔타이틀먼트 타입 [승계 §8-1]
`SubscriptionStatus` 8종 · `UserRecord` · `Entitlement`(effective_tier = paid OR grace) · `AccessTokenClaims`(15분, epoch 초) · `RefreshTokenClaims`(30일, jti 블랙리스트) · `NewUser` · `EntitlementPatch`(occurred_at 순서 가드) — v1 계약 §8-1/8-2 그대로. 시각 단위 경계 유지: JWT=초, DB·도메인=밀리초, 변환은 adapters.

### 2-3. 영속 도메인 타입 [신규 — 서버 정본화]
```ts
export interface SessionRec {            // 탐색 세션(진행 중 또는 완료)
  session_id: string; user_id: string;
  topic: string; area: string | null; domain_risk: DomainRisk;
  job_type: JobType[]; gap_type: GapType[] | null;
  user_condition: string | null; context_object: string | null;
  narrow: NarrowSnap | null;             // null이면 생성 완료(불변식: narrow 존재 ⟺ 생성 미완, v1 승계 — 삭제축과 독립)
  generated: Term[] | null;              // 생성된 리스트 전체(담기 0개여도 보존, FR-702)
  primer: PrimerDoc | null;              // [검토 정정] 프라이머 서버 정본. /summarize·/refine-primer가 갱신
  project_id: string | null; pinned: boolean;
  deleted_at: number | null;             // [검토 정정] 소프트 삭제. 목록 기본 제외, 유예 내 restore 가능
  created_at: number; updated_at: number;
}
export interface NarrowSnap {            // 좁히기 진행 스냅샷(매 턴 저장, FR-701)
  question: string; choices: Choice[];
  answers: { label: string; action: "선택" | "더깊이제외" | "어려워요"; at?: number }[];  // at = 클릭 시각(NFR-503 계측 자리, 선택)
  turns_left: number;                    // 재개 시 (현재 plan, answers)로 재계산이 정본(v1 교훈)
}
export interface AssetTerm {             // 담은 어휘 = 어휘 자산(FR-601)
  asset_id: string; user_id: string; session_id: string;
  term: Term;                            // 담은 시점의 카드 전체(상세 캐시 포함 가능)
  term_norm: string;                     // [검토 정정] 정규화 키. MindMap.nodes[].id = user 스코프 term_norm(세션 중복 담기는 맵에서 1노드로 병합)
  domain_tags: string[];                 // 마인드맵 크로스 도메인 엣지 재료(FR-312)
  project_id: string | null; created_at: number;
}
export interface KnowledgeState {        // 지식 상태(FR-502). unconfirmed는 클라 전용이라 없음
  user_id: string; term_norm: string;    // 정규화된 어휘 키
  tag: Tag; updated_at: number;
}
export interface Project { project_id: string; user_id: string; name: string; created_at: number }
export interface PrimerDoc {             // 구조화 프라이머(FR-604) — 서버 정본은 SessionRec.primer
  locale: OutputLocale;                  // [검토 정정] FR-952 — 생성 텍스트 로케일
  area: string; task_intent: string;
  user_condition?: string; context_note?: string;
  known_terms: string[]; unknown_terms: string[];
  refined?: { audience?: string; goal_detail?: string; constraints?: string[] };  // FR-607 산출
}
export interface MindMap {               // GET /map 응답(FR-312)
  scope: "all" | "project"; project_id?: string;
  nodes: { id: string; term: string; kind: string; group: string; session_id: string }[];
  edges: { from: string; to: string; kind: "group" | "relates_to" | "cross_domain" }[];
}
```
## 3. 엔드포인트 I/O 전체 [개정·신규]

### 3-1. 파이프라인 (v1 승계 + v2 신규)
| 엔드포인트 | In | Out | 게이팅 | 비고 |
|---|---|---|---|---|
| POST /classify | Prompt1In | Prompt1Out | free 주간 한도 **차감 시점**(TR-02) + 전역 캡 + IP리밋 | [승계] |
| POST /next | Prompt2In | Prompt2Out(enough, confidence) | 전역 캡 | [승계] |
| POST /preview | PreviewIn | PreviewOut | IP리밋만(경량) | [승계 v1 후반] |
| POST /relate | RelateIn | RelateOut | 전역 캡 | [승계 v1 후반] |
| POST /recommend | RecommendInput | SSE StreamEvent | 전역 캡 + domain_risk 재게이트 | [승계] 취소 신호 LLM까지 전파 |
| POST /detail | Prompt5In | Prompt5Out | free 서버 카운터(NFR-304) | [승계] RAG 캐시 적극 재사용(Q-09) |
| POST /summarize | Prompt4In | Prompt4Out(**PrimerDoc**) | pro 전용(402) | [개정] 출력 = 구조화 PrimerDoc |
| POST /refine-primer | RefinePrimerIn | RefinePrimerOut | pro 전용(402) | [신규 FR-607] |
| GET /config | — | ClientLimits | — | [승계] 운영 한도 외부화(NFR-504) |
| GET /map | scope, project_id? | MindMap | 로그인 필수 | [신규 FR-312] |

```ts
// [신규] 구조화 구체화 아키네이터(FR-607). 좁히기(P2)와 별개 세션·별개 프롬프트(P6).
export interface RefinePrimerIn {
  primer: PrimerDoc;                       // 현재 프라이머(초기엔 /summarize 산출)
  history: { label: string; action: "선택" }[];   // 이 아키네이터 안의 선택 누적
  free_input?: string;                     // 직접 입력 병행(합산 보장, FR-211 준용)
}
export interface RefinePrimerOut {
  question: string; choices: Choice[];     // 다음 다지선다(레이블 일상어, C-10 준용)
  enough: boolean; confidence: number;     // 종료 신호(엔벨로프 [2,5] — 좁히기보다 짧게, 실측 캘리브레이션 대상)
  primer: PrimerDoc;                       // 매 턴 갱신된 프라이머(refined 채워짐)
}
```

### 3-2. 인증 [승계 §8-4 개정 — 폼팩터 재설계(NFR-307)]
| 엔드포인트 | In | Out | 비고 |
|---|---|---|---|
| POST /auth/google | { code, code_verifier, redirect_uri, **platform: "web"\|"desktop"** } | { access, refresh, user } | [검토 정정] Google OAuth 클라이언트는 타입별 등록이라 서버가 platform으로 client_id/secret 쌍을 선택. PKCE, id_token iss/aud 검증 |
| POST /auth/refresh | { refresh } | { access, refresh } | jti 블랙리스트 검사(refresh 시점만, networkless access 검증 유지) |
| POST /auth/logout | { refresh } | 204 | jti 블랙리스트 등록 |
| GET /subscription/status | (Bearer) | Entitlement | [승계] |
| POST /checkout | (Bearer) | CheckoutSession 또는 coming_soon | [승계] PAYMENT_ENABLED b안 유지(TR-04) |
- 웹: SPA → OAuth redirect(자기 오리진 콜백). 데스크톱(Tauri): 시스템 브라우저 + 루프백/커스텀 스킴 콜백 — 구현은 adapters/tauri, 코어 계약은 동일(code+PKCE).
- launchWebAuthFlow·chrome.identity 코드는 이식 금지(v1 잔재).

### 3-3. 영속 CRUD [신규 — 서버 정본. 전부 로그인 필수(Bearer), 자기 소유 행만]
| 엔드포인트 | 동작 |
|---|---|
| GET /sessions?scope=… | 세션 목록(검색 q, project_id, pinned 필터. 커서 페이지네이션) |
| GET /sessions/:id | 단건(narrow·generated 포함) |
| PUT /sessions/:id | 진행 스냅샷 upsert(매 턴 저장, 멱등 — 클라 생성 session_id=UUIDv7) |
| DELETE /sessions/:id | 소프트 삭제(deleted_at 세팅) |
| POST /sessions/:id/restore | [검토 정정] 유예 내 실행취소(FR-703). 유예 경과 후 404 |
| GET /usage | [검토 정정] 잔여 사용량(주간 탐색 잔여·detail 잔여) — TR-08 UI 표면. 익명은 추정치 플래그 동반 |
| PUT /sessions/:id/keep | 담기: AssetTerm 생성/해제 |
| GET /assets?project_id=… | 어휘 자산 목록(커서 페이지네이션 — Data API 1MB 상한 대응, term JSONB는 목록에서 요약 필드만) |
| GET/POST/DELETE /projects | 프로젝트 CRUD(삭제 시 세션 소속만 해제, 세션 보존 — v1 규칙 승계) |
| PUT /knowledge | KnowledgeState 일괄 upsert(태깅 변경 배치) |
- **동기화 규칙(FR-503·FR-902):** 정본=서버. 클라(웹 IndexedDB/localStorage, 데스크톱 로컬 캐시)는 read-through 캐시 + 오프라인 열람용. 쓰기는 online-only(오프라인 쓰기 큐는 범위 밖 — 생성 기능 자체가 온라인 전용이므로 충돌 표면 최소). 충돌 시 updated_at 큰 쪽이 아니라 **서버 수리 원칙**: 클라는 서버 응답으로 자기 캐시를 덮는다.
- **소유권 규칙(검토 정정):** PUT /sessions/:id는 해당 session_id가 이미 **다른 user_id 소유면 409로 거부**한다(횡령 방지). 익명→로그인 승격 업로드 시 서버가 **새 session_id를 발급**하고 클라 임시 id는 client_ref로만 기록(공유 기기에서 타인 로컬 세션의 무증명 편입 차단 + UUID 충돌 문제 동시 해소).
- **미로그인 사용자:** 파이프라인은 익명 허용(free 한도, NFR-301). 영속 CRUD는 로그인 필수 — 미로그인 세션은 클라 로컬에만 임시 보관하고 로그인 시 서버로 승격(위 승격 규칙).
- **FR-706 dedup 책임(검토 정정):** 서버 책임이다 — /recommend가 세션의 project_id로 프로젝트 자산 term_norm을 조회해 exclude에 병합한다(클라 전달 의존 금지).

## 4. 게이팅·한도 의사코드 [승계 개정]
```
전 엔드포인트 공통 미들웨어(검토 정정 — 개별 게이트 아님):
  clientCheck:     프로덕션에서 웹=Origin 화이트리스트, 데스크톱=클라이언트 인증 토큰(방식은 C4 스펙 확정) — NFR-305
  riskKeywordGate: 서버 정적 키워드 매핑 1차 방어(LLM 판정 이전) — NFR-306. classify·recommend 입구에 적용
  globalDailyCap → ipRateLimit: 모든 비용 발생 엔드포인트(/next·/preview·/relate·/recommend 포함)
resolveTier(req):  Bearer JWT 검증(HS256, exp) → claims.tier   // networkless, x-tier류 헤더 완전 무시(C-21)
                   DEV_MODE && DEV_FORCE_TIER → override(로컬 전용)
gate(/classify):   freeWeekly(user_id 또는 익명 키) 차감 → 통과.
                   ★재차감 금지(TR-02): 세션 재개(PUT /sessions + /next 루프)는 /classify를 다시 호출하지 않는 흐름으로 강제 — 차감은 오직 신규 탐색 시작뿐
gate(/detail):     free면 서버 카운터 증가·상한 검사(NFR-304)
gate(/summarize, /refine-primer): tier != paid → 402
사용량 저장:        Upstash(레이트리밋·주간 카운터) — 장애 시 fail-open(NFR-404). GET /usage가 같은 카운터를 읽는다
```
- 익명 키 전략: IP+day 기반(NFR-301). 계정 기반 정밀 카운트는 로그인 사용자에 적용.

## 5. 스트리밍 프로토콜 [승계 + 검토 정정]
- /recommend: `text/event-stream`. 이벤트 = `term`(Term 1건) · `meta`(terms_rationale 등) · `done` · `error`(정화된 메시지). Lambda 어댑터는 streamHandle로 동일 이벤트 직렬화(adapters/aws), 로컬은 node-server SSE. **클라 AbortController → fetch signal → 파이프라인 → LlmClient.streamTerms까지 전파**(FR-310).
- ★**aws 계층 취소 제약(검토 정정, AWS 공식 문서 확인):** Lambda 응답 스트리밍은 클라이언트 연결 끊김을 함수에 통지하지 않는다 — fetch signal 경로는 local에서만 즉효다. 따라서 FR-310의 계약을 계층별로 명시한다: **local = 즉시 중단 보장 / aws = 클라 측 수신 중단은 항상 보장하되 서버 측 LLM 중단은 보완 메커니즘으로 달성**(term 이벤트 flush 실패 감지 시 중단, 스트림당 토큰·개수 상한). 보완 설계는 C2 스펙 확정 항목이며, "3계층 동일 메커니즘"이라는 서술은 폐기한다.
- 이벤트 스키마·순서는 mock(클라 목)도 동일하게 재현한다 — mock 스키마는 shared 이벤트 타입에서 파생하고 C3 게이트로 대조(§0-2).
## 6. 데이터베이스 스키마 (PostgreSQL 정본) [신규 — v1 D1 0001_init 승계 확장]

```sql
-- users: v1 D1 K1 스키마 승계(컬럼 동일 취지), PG 타입으로. PK=내부 user_id.
CREATE TABLE users (
  user_id        TEXT PRIMARY KEY,           -- UUIDv7
  email          TEXT NOT NULL UNIQUE,
  google_sub     TEXT UNIQUE,
  locale         TEXT NOT NULL DEFAULT 'ko', -- 검토 정정: FR-952 언어 설정 영속(크로스 플랫폼)
  tier           TEXT NOT NULL DEFAULT 'free',
  subscription_status TEXT NOT NULL DEFAULT 'none',
  expires_at BIGINT, current_period_end BIGINT,
  cancel_at_period_end BOOLEAN NOT NULL DEFAULT FALSE,
  grace_until BIGINT, failed_payment_count INT NOT NULL DEFAULT 0,
  next_retry_at BIGINT, last_failure_code TEXT,
  current_price INT, currency TEXT, billing_interval TEXT NOT NULL DEFAULT 'monthly',
  pg_provider TEXT, created_at BIGINT NOT NULL
);
CREATE TABLE sessions (
  session_id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(user_id),
  topic TEXT NOT NULL, area TEXT, domain_risk TEXT NOT NULL,
  job_type JSONB NOT NULL, gap_type JSONB,
  user_condition TEXT, context_object TEXT,
  narrow JSONB,                                -- NarrowSnap. NULL ⟺ 생성 완료
  generated JSONB,                             -- Term[]
  primer JSONB,                                -- PrimerDoc 서버 정본(검토 정정)
  project_id TEXT REFERENCES projects(project_id) ON DELETE SET NULL,   -- 검토 정정: FK + 소속 해제 자동화
  pinned BOOLEAN NOT NULL DEFAULT FALSE,
  deleted_at BIGINT,                           -- 소프트 삭제(실행취소)
  created_at BIGINT NOT NULL, updated_at BIGINT NOT NULL
);
CREATE INDEX idx_sessions_user ON sessions(user_id, updated_at DESC) WHERE deleted_at IS NULL;
CREATE TABLE assets (
  asset_id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(user_id),
  session_id TEXT NOT NULL, term JSONB NOT NULL,
  term_norm TEXT NOT NULL,                     -- 정규화 키(중복 담기 방지·맵 노드 키)
  domain_tags JSONB NOT NULL,
  project_id TEXT REFERENCES projects(project_id) ON DELETE SET NULL,   -- 검토 정정: FK
  created_at BIGINT NOT NULL,
  UNIQUE(user_id, session_id, term_norm)
);
CREATE INDEX idx_assets_user_project ON assets(user_id, project_id);
CREATE TABLE knowledge (
  user_id TEXT NOT NULL REFERENCES users(user_id),
  term_norm TEXT NOT NULL, tag TEXT NOT NULL, updated_at BIGINT NOT NULL,
  PRIMARY KEY(user_id, term_norm)
);
CREATE TABLE projects (
  project_id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(user_id),
  name TEXT NOT NULL, created_at BIGINT NOT NULL
);
-- 결제 이벤트·billing_locks·notification_log·consents: v1 K1 승계(Phase 1 활성화, 컬럼 정의는 유지)
```
- JSONB 채택 근거: narrow/generated/term은 클라 정본 구조의 스냅샷이라 정규화 이득이 작고 스키마 진화가 잦다. 조회 축(사용자·프로젝트·시간)만 컬럼·인덱스로.
- 테이블 생성 순서(FK): users → projects → sessions → assets → knowledge.
- 마이그레이션: `adapters/*/migrations/` 순번 SQL. 로컬(Docker PG)과 Aurora가 같은 파일을 사용.
- **Data API 제약(검토 반영, AWS 공식):** ①트랜잭션은 3분 유휴 시 자동 롤백 — 다단계 트랜잭션은 짧게 설계하고 장기 로직에 트랜잭션 걸치기 금지(리포 구현 규칙) ②응답 1MB 상한 — 목록 API는 전부 커서 페이지네이션 필수(**GET /assets 포함**, §3-3에 적용), JSONB 대형 필드는 목록 응답에서 제외(단건 조회로만).

## 7. 패키지 파티션 맵 (ADR-004 확정본) — 폴더 = 스펙 = 빌드 단위
```
v2/app/
├ packages/shared/        본 SoT의 타입·포트·프롬프트 빌더 시그니처(§2·§3·§8). 웹표준만
├ packages/core/          파이프라인(P1~P6)·RAG·로케일·게이팅 순수 로직. 웹표준만
├ packages/ui-shared/     화면(entry·narrow·preview·terms·detail·summary·refine·map·sessions)·훅·SSE 클라·i18n. 플랫폼 API는 props/포트 주입
├ packages/adapters/http-app/  Hono 앱 조립(라우트↔core 배선). 부트 없음
├ packages/adapters/aws/       Lambda 부트(streamHandle)·Data API 리포 구현·Secrets·EventBridge 핸들러
├ packages/adapters/local/     node-server 부트·node-postgres(기본)/SQLite(대안) 리포·인메모리 캐시
├ packages/adapters/tauri/     파일첨부·알림·전역단축키(우아한 강등)·오프라인 캐시·업데이터 배선
├ packages/web/           Vite SPA 셸(라우터·오리진 콜백·광고 슬롯[free])
├ packages/desktop/       Tauri 셸(동일 SPA 번들 + tauri 어댑터 주입)
├ packages/landing/       Astro(설득 콘텐츠·가이드 — AdSense 심사 대상 영역)
└ packages/scripts/       경계 게이트 3종·마이그레이션 러너·스모크
```
- 의존 방향(강제): shared ← core ← http-app ← {aws, local} / shared ← ui-shared ← {web, desktop} / landing은 독립(+디자인 토큰만 공유). **역방향·형제 간 직접 참조 = 빌드 실패.**

## 8. 프롬프트 자산 경계 [신규 — NFR-311]
- 프롬프트 빌더 본문(buildPrompt1~6·preview·relate)은 **core 패키지에만** 존재한다. shared에는 시그니처(입출력 타입)만.
- ui-shared·web·desktop·landing 번들에 프롬프트 문자열이 포함되면 안 된다 — portability-guard에 프롬프트 토큰 검사 추가(경계 게이트의 일부).
- API 에러 응답은 정화 계층을 거쳐 프롬프트·내부 경로를 절대 노출하지 않는다(v1 에러 정화 승계).
- 시스템 프롬프트 유출 유도 입력 방어 지시를 P1~P6 공통 가드 블록으로 둔다.

## 9. 구현 사이클 (buildflow, 각 사이클 = 커밋 게이트 + 스펙 재동기화)

> 코드 작성 하드 규칙(파일 단일 책임·크기 상한·배럴 인덱싱·패턴 강제 지점) = `코드규약.md`. 모든 구현자 필독이며 위반 0이 전 사이클 게이트에 포함된다.

| 사이클 | 범위 | 완료 기준(게이트) |
|---|---|---|
| C1 뼈대 | pnpm 모노레포 + shared/core 이식(v1에서) + 경계 게이트 3중 + 코드규약 검사(파일 크기·배럴 경유) + local 부트로 mock LLM 파이프라인 관통 | check(tsc -b 정상 배선)+가드+cruiser+규약 검사 전부 PASS, local에서 /classify→/next→/recommend(mock) e2e |
| C2 백엔드 | adapters/local 완성(PG 리포)·adapters/aws(Lambda+Data API)·인증 4종·게이팅 | 로컬 PG e2e + AWS staging 배포 스모크(실 LLM 1회) |
| C3 웹 | ui-shared 분리(반응형 재설계)+web 셸+landing | 5180급 시각 확인(사용자)+핵심 여정 e2e |
| C4 데스크톱 | tauri 어댑터+desktop 셸+고유 기능 5종+서명·업데이터 | Windows·Linux 패키징 스모크 |
| C5 신규·수익 | 마인드맵·refine-primer·광고(애드핏)·결제 재설계 착수 | 기능별 인수 시나리오 |
- 각 사이클 착수 전 그 사이클의 패키지별 스펙(≤400행)을 본 SoT에서 파생 작성 → 적대 검토 → 구현(worktree 병렬은 독립 패키지만).

## 10. 열린 항목 (구현 중 점검 — 담당 사이클 명시)
- [C2] aws 취소 보완 메커니즘(§5) 설계·실측 + Aurora cold resume·Lambda GB-초 계측 + Data API 트랜잭션 규칙 준수 검증.
- [C2] enough [3,8] 캘리브레이션·exclude 페이지네이션 중복/고갈 검증(v1 §7 미해결 승계) + Aurora 읽기 일관성(stale read) 확인(v1 §9-11의 D1 제약 재검토).
- [C2] Google OAuth 웹/데스크톱 client_id 이원화의 콘솔 실등록 확인(F6은 중하 신뢰 — 실측으로 확정).
- [C3] mock↔local 이벤트 등가성 게이트 구현(§0-2).
- [C5] refine-primer 엔벨로프 [2,5]·P6 품질 캘리브레이션 + 마인드맵 cross_domain 임계 실측.

## 11. v1 계약 이월 절 (검토 정정 — 침묵을 "의도된 유예"로 명시)
아래는 본 SoT가 지금 재기술하지 않으나 **폐기가 아니다.** 해당 사이클 스펙 파생 시 v1 계약 원문을 필수 입력으로 재확정한다.
- **[C2 이월] v1 §8 인증·엔타이틀먼트 세부:** JWT 키 로테이션(JWT_SECRET_PREV·kid 이중검증), refresh 시 DB 최신 tier 재조회, /subscription/status의 access_token 재발급 반환, 에러 코드 표(AUTH_FAILED/TOKEN_REVOKED/TOKEN_EXPIRED/ALREADY_SUBSCRIBED/PAYMENT_DISABLED), DEV_MODE 가드 세부. 블랙리스트 저장소는 v1의 "D1 revoked_jtis" 결정을 Aurora 전환에 맞춰 재결정(Aurora 테이블 vs Upstash — C2 스펙).
- **[C5 이월] v1 §9 결제 Phase 1 계약 전체:** 포트원 V2 어댑터(결제창 SDK 전용·PG별 빌링키 분기·예약결제·webhook HMAC·FSM·pending_checkout/revoked_jtis/schedule_log 테이블·users.last_event_at), 법무 조항(청약철회 7일·사전동의·국외이전 고지·만14세 차단·VAT 표기). ★결제 재설계(SRS TR-06)는 v1의 이 검증된 조사·결정을 **입력으로 재사용**하며 백지 재조사를 금지한다(웹 결제라 오히려 제약이 줄어든 부분만 재평가). webhook 수신 라우트(POST /webhook/payment)는 C5에서 TR-07(멱등·순서역전) 적용 지점으로 신설.
```
