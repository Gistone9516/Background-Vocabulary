---
원본: v1/docs/배경어휘-사이드탭-인터페이스계약.md (385행~451행, 실제 파일 끝. §9-6 꼬리~§9-12)
정본: 원본 파일이 정본이며 이 원장은 v2 입력용 인벤토리다
---

> 비고: 지시된 예상 끝행(약 450행)과 실제 파일 끝(451행)이 1행 차이로 불일치. 실제 파일 끝(451행, "MINOR" 단락 마지막 문장까지)을 기준으로 전량 처리함. 451행 이후 내용 없음(파일은 451행에서 종료).

### 02c-1. 구독 상태 FSM 전체 상태값
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-6 (385행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > `none → active → (past_due → grace) → canceling → expired`, + `suspended`(분쟁)·`refunded`.

### 02c-2. 단일 원자 갱신 제약 (R1)
- 유형: 제약
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-6 (386행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **단일 원자 갱신(R1):** tier·subscription_status·expires_at·grace_until·last_event_at를 **하나의 SQL UPDATE 또는 db.batch**로(복수 독립 prepare().run() 금지). `applyEntitlement` 포트 주석에 제약 명기.

### 02c-3. graceful cancel 방식 (R1 missing_axis)
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-6 (387행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **graceful cancel(R1 missing_axis):** 해지 요청 = `cancel_at_period_end=true`(즉시 free 아님). current_period_end까지 pro 유지 → 경량 Cron(9-7)이 기간 종료 시 tier=free + `cancelSchedules`+`deleteBillingKey`.

### 02c-4. 유효 pro 판정식
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-6 (388행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 유효 pro = `effectiveEntitlement`(tier=paid OR grace_until>now).

### 02c-5. 정기결제 갱신 방식 — 포트원 예약결제 구동 (자체 월청구 Cron 폐기)
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-7 (390행~391행)
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요) — 결제 게이트웨이(포트원) 자체는 폼팩터 무관이나 9-10 클라 UI 배선과 연동되므로 재검토 표시 병기
- 원문: > ### 9-7. 정기결제·dunning (1b, K2) — 포트원 예약결제 구동 + 경량 Cron (R1 채택)
> - **갱신(자체 월청구 Cron 폐기):** 가입·매 결제 성공 시 다음 달 timeToPay로 `schedulePayment` 등록 → 포트원이 청구·`Transaction.Paid` webhook → 9-5가 period 연장 + 다음 달 재예약. **"오늘 청구할 사람 찾기" 쿼리 자체가 없어 DATE()/UTC·KST/billing_locks/CPU 버그를 통째로 회피.**

### 02c-6. 경량 일일 Cron 사양
- 유형: 기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-7 (392행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **경량 일일 Cron**(`wrangler.toml [triggers] crons`, `scheduled` 핸들러 export 필요): ① grace 만료자 강등 ② `cancel_at_period_end` 기간 종료자 강등 ③ reconciliation(9-8). 시각 비교는 **정수**(`current_period_end <= unixepoch()*1000` — R1: epoch ms에 SQLite `DATE()` 쓰면 NULL → 전원 미갱신 조용한 실패). 1회 처리량 상한(예 ≤50건)+잔여 다음 주기.

### 02c-7. grace 처리 규칙
- 유형: 기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-7 (393행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **grace:** Failed webhook 시 past_due·failed_count++. pgMessage가 카드만료/재등록류면 즉시 grace + 카드재등록 이메일. 일시실패는 다음 `schedulePayment`를 D+1/D+3/D+6로 재예약([확인필요: 포트원 예약 자동재시도 — 미지원이면 우리가 재예약]). grace 3~7일 만료+소진 → 강등. effective_tier는 grace 중 pro 유지.

### 02c-8. 확인필요 항목 — 포트원 예약 자동재시도 여부
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-7 (393행, 393행 내 대괄호 표시)
- 폼팩터: 재검토(폼팩터 전환으로 의미 변할 수 있음) — §9-12에서 확인 완료로 폐기됨(02c-19 참조)
- 원문: > [확인필요: 포트원 예약 자동재시도 — 미지원이면 우리가 재예약]

### 02c-9. 강등 순서 (R1)
- 유형: 제약
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-7 (394행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **★강등 순서(R1):** `cancelSchedules(billingKey)` → `deleteBillingKey` → tier=free. 첫 단계 실패 시 status=degradation_pending + 운영 알림(예약 남은 빌링키 삭제는 에러).

### 02c-10. reconciliation 배치 사양
- 유형: 기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-8 (396행~397행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 9-8. reconciliation (1b, K3)
> 경량 일일 Cron(9-7)에 통합하거나 ≥1h 간격(R1: <1h scheduled 핸들러는 CPU 30초 한도 → 전수조회 시 조용한 타임아웃). **전수 스캔 금지 → 필터:** 최근 24h 예약 청구건 중 webhook 미수신(payment_events에 해당 paymentId Paid 없음)·`REQUESTED` stuck 건만 `getPayment` 단건조회로 동기화. 1회 ≤20건, 잔여 다음 주기. 멱등.

### 02c-11. 청약철회 제한 표시 의무 (전자상거래법 §17②, R1 BLOCKING)
- 유형: 비기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (399행~400행)
- 폼팩터: 재검토(폼팩터 전환으로 의미 변할 수 있음) — 법규 요건 자체는 무관이나 "체크박스" UI 구현은 재설계 대상
- 원문: > ### 9-9. 동의·법규·서버 미터링·이메일 (1b, A6·K6 — R1 채택)
> - **★청약철회 제한(전자상거래법 §17②, R1 BLOCKING):** 체크박스만으론 효력 無. 결제·동의 화면에 **구독 즉시개시 + 청약철회 불가 사실을 별도 색·크기로 명확 표시**, 약관에 '즉시개시·철회불가 사유' 조항 명기 후 체크박스를 그 조항에 연결(시험사용 어려우면 사유 명시).

### 02c-12. 사전동의 수령 원칙 (§13⑥·시행령 §20조의2, R1)
- 유형: 비기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (401행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **★사전동의는 "수령"이지 "발송"이 아님(§13⑥·시행령 §20조의2, R1):** 무료→유료 14일 / 대금 인상 30일 전 **명시적 동의 확인**. 무응답 ≠ 동의 → `consents`에 동의 완료 기록, **미동의자는 전환·인상 미적용 분기**.

### 02c-13. 국외이전 고지 방식 (개보법 §28조의8①③, R1)
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (402행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **국외이전(개보법 §28조의8①③, R1):** 별도 동의 대신 **개인정보처리방침에 수령자별(Cloudflare 미국·포트원·DeepSeek·Upstash) 수령국·연락처·목적·보유기간 공개**로 처리(별도 동의 체크박스 회피). 별도 동의 유지 시 수령자별 낱낱 고지(묶음 1체크 위법 소지).

### 02c-14. 미성년자 제한 (개보법 §22조의2·민법 §5, R1) — 9-12에서 강화됨(02c-20 참조)
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (403행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **미성년자(개보법 §22조의2·민법 §5, R1):** 자기신고 불충분. 약관으로 **만14세 미만 유료 구독 제한** 명시 + 동의 모달에 '만14세 미만 보호자 동의 필요' 표시(미성년 계약 취소 리스크).

### 02c-15. 국내 VAT 표시 의무 (R1 MISSING)
- 유형: 비기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (404행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **국내 VAT(R1 MISSING):** ₩3,900 = **부가세 10% 포함가** 명시(공급가 ₩3,545 + VAT ₩355). 영수증 = 포트원 영수증 URL(카드 매출전표), 세금계산서 수동.

### 02c-16. 갱신 D-14 통지 성격 정정 (R1 WRONG)
- 유형: 교훈·경위
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (405행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **갱신 D-14 통지 = 법적 의무 아님(R1 WRONG)** → **자체 정책/UX**로 분리 표기(법적 의무는 청약철회 7일·무료→유료 14일·인상 30일뿐).

### 02c-17. 서버 미터링·이메일 어댑터·운영 알림
- 유형: 기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-9 (406행~408행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **서버 미터링(A6):** 주간 한도·detail 횟수를 `user_id` D1 카운터로(클라 신뢰 제거). 무인증은 기존 IP·익명.
> - **이메일 어댑터(`adapters/workers/src/email/`):** `EmailPort{ send(to,subject,html) }`, Resend/CF. `notification_log`(user+type+period) 멱등.
> - **운영 알림(K8):** 중복청구·환불급증·chargeback·degradation_pending → `OPS_ALERT_URL`.

### 02c-18. 클라이언트 UI 배선 (extension) — 페이월 CTA·동의 모달·manifest
- 유형: 기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-10 (410행~413행)
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요) — `chrome.tabs.create`·manifest `host_permissions`·`tabs` 권한은 확장 API 전용, 웹사이트/데스크톱은 전면 재설계 필요
- 원문: > ### 9-10. 클라이언트 UI (extension, 1a 중심)
> - 페이월 CTA = `onPaywallCta` 확장: 로그인 → 동의 모달 → `/pay/intent` → `chrome.tabs.create` → 폴링. (Phase 0의 coming_soon 분기는 PAYMENT_ENABLED=false일 때 유지.)
> - 동의 모달: **청약철회 불가·즉시개시 명확 표시(별도 강조, R1)** + 약관/개인정보 링크 + 동의 체크(약관·자동갱신; 국외이전은 처리방침 안내) + **만14세 미만 보호자 동의 안내**. 가격 **₩3,900(VAT 포함) / $2.99(세금 별도)** 수동 통화 전환(K7). 폴링 진행·타임아웃 안내, 실패 토스트+재시도, 복구(재로그인→/subscription/status). i18n 4언어 가산.
> - manifest: `host_permissions`에 결제 탭 도메인(cdn.portone.io·PG 도메인은 탭이 일반 웹이라 불요), `tabs` 권한(이미 chrome.tabs.create는 권한 불요).

### 02c-19. 불변·범위 — 어댑터 경계·D1 stale read·billingKey 저장·기존 계약 불변
- 유형: 불변식
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-11 (415행~419행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 9-11. 불변·범위·게이트
> - 어댑터·webhook·Cron·이메일 = adapters에만. core·shared D1/PG 참조 0(이식성). `PaymentGatewayPort`=shared(Phase 0), 구현만 adapters.
> - **D1 stale read(R1):** 쓰기 직후 읽기(webhook upsert → 폴링 /subscription/status)는 **D1 Sessions API 동일 세션** 또는 읽기복제 비활성(wrangler.toml)으로 stale 방지.
> - **billingKey 저장(R1):** 포트원 opaque 토큰(카드 원본 아님, 청구엔 API secret 필수). D1 `billing_key` 컬럼, 접근은 어댑터 한정.
> - 기존 파싱 계약·5엔드포인트·theme.css·i18n 4언어 불변.

### 02c-20. PAYMENT_ENABLED 하드플로어 조건 (§K9)
- 유형: 제약
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-11 (420행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **PAYMENT_ENABLED 하드플로어(§K9):** 사업자등록 + (통신판매업 신고 **또는 면제** — 직전년 거래 50회 미만·간이과세자면 면제, R1) + 약관/개인정보 게시 + 이메일 인프라 + 1b(예약·grace·reconciliation) 완료 + 14/30일 **동의 수령** 경과. 코드 완성 ≠ 활성화.

### 02c-21. 근거 출처 목록 (§9-11)
- 유형: 근거·수치
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-11 (421행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 출처(load-bearing): developers.portone.io/{api/rest-v2,sdk/ko/v2-sdk,opi,blog} · github.com/portone-io/server-sdk · standardwebhooks.com · developers.cloudflare.com/{workers,d1} · owasp.org · 전자상거래법·개인정보보호법·국세청. (deepflow R1 run wf_cd0ae62b-8e8 findings 출처 대조 채택.)

### 02c-22. §9-12 도입부 — R2 보강 원칙
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (423행~424행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 9-12. deepflow R2 반영 — 보강·확정 (run wf_b6301fe3-aa3, 출처 대조)
> R2(수정 §9 재비판)에서 NEW 확인된 결함과 잔여 확인필요 해소. **아래가 위 9-0~9-11을 보강·우선**한다.

### 02c-23. 잔여 확인필요 확정 4건 (웹근거)
- 유형: 근거·수치
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (426행~430행)
- 폼팩터: 재검토(폼팩터 전환으로 의미 변할 수 있음) — 페이레터/엑심베이 채널 선택은 v2에서도 유효하나 v2 결제 프로바이더 재확정 시 재검증 필요
- 원문: > **잔여 [확인필요] 확정(웹근거):**
> - Authorization `PortOne {secret}` 직접 = **공식 auth 문서 명문**(확정, Bearer 폴백 병존).
> - 포트원 V2 예약결제 실패 = **자동재시도 없음**(V1 콘솔 전용) → Failed webhook 후 **가맹점이 직접 재예약**(9-7 grace가 정답). 9-0 [확인필요] 폐기.
> - Idempotency-Key = **RFC 8941 쌍따옴표 필수**(예: `"charge-{uid}-{YYYYMM}"`), 통합테스트 실측.
> - 페이레터: 빌링키 정기결제는 지원하나 **서버 재청구(billing-key/schedule) V2 지원이 공개문서 미확인** → **USD 채널 기본 = 엑심베이**, 페이레터는 포트원 서면 확인 후 추가.

### 02c-24. 신규 D1 테이블 목록 (migration 0002, Phase 1)
- 유형: 기능요구
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (432행~436행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **신규 D1 테이블 (migration 0002, Phase 1):**
> - `pending_checkout(sid PK, user_id, payment_id, amount, currency, channel_key, created_at)` — checkout 락+파라미터(S1). TTL 네이티브 없음 → 읽기 시 `created_at+600000<now` 만료검사 + 경량 Cron 정리. `/pay/params` = **`DELETE ... WHERE sid=? RETURNING *`(affected=0→404)** 원자 단일소비(S4). `/pay/intent`는 `preRegister`(외부 콜) 실패 시 pending_checkout DELETE 롤백 후 5xx.
> - `revoked_jtis(jti PK, expires_at)` — **jti 블랙리스트 = D1**(Upstash async 미스 window 제거, S2). `/auth/refresh`가 여기서 검사, Cron이 만료행 정리. user_id Upstash blacklist는 보조.
> - `schedule_log(payment_id PK, user_id, billing_key, time_to_pay, status)` — 예약 추적(heartbeat·dunning 기준).
> - `users += last_event_at`(순서가드, 9-5).

### 02c-25. BLOCKING 보강 (O1) — Paid webhook 유실 → heartbeat
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (438행~439행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **★BLOCKING 보강:**
> - **(O1) Paid webhook 영구유실 → 다음 예약 미등록 → 조용한 매출 중단:** 경량 Cron에 **heartbeat** — active·paid 중 `schedule_log`에 미래 SCHEDULED 없는 행 탐지 → `schedulePayment` 재등록 + 운영 알림(≤20건/회). Paid 핸들러 재예약 실패는 Upstash 재시도 큐.

### 02c-26. BLOCKING 보강 (L1) — 만14세 미만 가입 차단
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (440행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - **(L1 만14세, 개보법 §22조의2):** 모달 '안내 표시'만으론 위법(법정대리인 동의 실제 수령·확인 의무). → **가입 시 생년월일 자기신고 후 만14세 미만이면 가입 차단**(1인 운영 현실안), 약관·모달 명시. (법정대리인 확인 인프라 비도입.)

### 02c-27. MAJOR 보강 (L2) — 청약철회 즉시개시 동의 근거
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (442행~443행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **MAJOR 보강:**
> - **(L2 청약철회 §17②):** "사유 명시"는 법령 표현 아님 → **시험사용 제공(현 free tier 주7회 = 한시적 이용)** 또는 콘텐츠 정보 제공 중 택일로 즉시개시 동의 근거 연결. 동의 모달 문구 교정.

### 02c-28. MAJOR 보강 (O2) — graceful cancel 이중청구 방지
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (444행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - **(O2 graceful cancel 이중청구):** 해지 요청 **즉시 `cancelSchedules`**(미래 예약 취소) 후 `cancel_at_period_end=true`. Cron은 period_end에 `deleteBillingKey`+free만(예약 이미 소거). cancelSchedules 실패=cancellation_pending+알림.

### 02c-29. MAJOR 보강 (O4) — dunning 미시작 방지 reconciliation 기준 변경
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (445행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - **(O4 Failed webhook 유실 → dunning 미시작):** reconciliation(9-8)을 `schedule_log` 기준으로 — time_to_pay 경과인데 Paid·Failed 둘 다 미수신 건을 `getPayment` 동기화, FAILED면 dunning 재예약.

### 02c-30. MAJOR 보강 (S3) — webhook db.batch 실패 시 명시적 500
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (446행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - **(S3 webhook db.batch 실패):** D1 오류·타임아웃 시 **명시적 500**(포트원 5회 재전송 유도), 핸들러 try/catch 필수.

### 02c-31. MAJOR 보강 (A3) — db.batch affected=0 처리
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (447행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - **(A3 db.batch affected=0):** batch는 mid-batch 조건부 롤백 불가 → 앱 레이어에서 `results[users].meta.changes===0`이면 역전·중복으로 보고 후속(재예약 등) **건너뜀**. payment_events는 감사용 INSERT OR IGNORE.

### 02c-32. MAJOR 보강 (S5) — /pay/complete D1 직접 write 금지
- 유형: 제약
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (448행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - **(S5 /pay/complete):** **D1 직접 write 금지**(권위=webhook). KRW 초회 `chargeWithBillingKey` 결과는 Upstash TTL(폴링 노출)에만.

### 02c-33. MINOR 보강 — USD 분기 주석·D1 Sessions API 캡슐화
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-인터페이스계약.md §9-12 (450행)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > **MINOR:** USD 분기 주석 = "엑심베이·페이레터 모두 `requestIssueBillingKeyAndPay`"(M1). D1 Sessions API는 `D1UserRepository`(adapters) 내부 캡슐화, `UserRepository` 포트에 세션 노출 금지(A4 이식성).

## 검증 보충

### 검증 방법
원본 파일(v1/docs/배경어휘-사이드탭-인터페이스계약.md) 385행~450행 전체를 Read로 재독하고, `wc -l`·python `readlines()`로 실제 파일 길이를 재확인한 뒤(정확히 450줄, 최종 줄은 "**MINOR:** USD 분기 주석..." 문장), 원장(02c-1~02c-33)의 모든 "원문:" 인용 블록을 추출해 원본 385~450행의 공백 아닌 55개 줄(빈 줄 11개 제외)과 python `difflib`로 1:1 대조했다.

### (1) 누락 검사 결과
누락 없음. 원본의 요구·결정·수치·제약·기각대안·확인필요 항목이 전부 원장에 대응 엔트리로 존재함을 확인:
- §9-6 꼬리(385~388행, FSM·원자갱신·graceful cancel·유효pro 판정) → 02c-1~02c-4
- §9-7 정기결제·dunning(390~394행, 갱신방식·경량Cron·grace·강등순서, 확인필요 포함) → 02c-5~02c-9
- §9-8 reconciliation(396~397행) → 02c-10
- §9-9 동의·법규·미터링·이메일(399~408행, 청약철회·사전동의수령·국외이전·미성년자·VAT·D-14통지·서버미터링·이메일어댑터·운영알림) → 02c-11~02c-17
- §9-10 클라 UI 배선(410~413행) → 02c-18
- §9-11 불변·범위·게이트(415~421행, 어댑터경계·D1 stale read·billingKey저장·기존계약불변·PAYMENT_ENABLED 하드플로어·출처목록) → 02c-19~02c-21
- §9-12 R2 반영(423~450행, 확인필요 4건 확정·신규D1테이블 3종+컬럼추가·BLOCKING 2건·MAJOR 6건·MINOR) → 02c-22~02c-33

### (2) 요약화 검사 결과
요약·의역·축약 없음. 원장의 모든 "원문:" 인용이 원본 문장을 축약 없이 그대로 재현하고 있음을 diff로 확인(★·굵게·화살표·대괄호 확인필요 표기 포함 전부 보존). 유일한 형식 차이는 원장이 단독 인용 항목에서 원본의 목록 기호("- ")를 생략한 것뿐이며(예: 02c-2가 원본 386행 "- **단일 원자 갱신(R1):**..."에서 선행 "- "를 뺌), 내용·의미 손실은 없어 교체 대상 아님.

### 메타 정보 정정 (내용 아님, 참고)
원장 상단 frontmatter(2행)와 비고(6행)가 "실제 파일 끝(451행)"이라 적었으나, `wc -l`과 python `readlines()` 재확인 결과 원본 파일은 정확히 **450줄**이며 450행이 실제 파일 끝(§9-12 MINOR 문장)이다. 인용된 개별 행 번호(385~450)는 모두 정확하므로 이 항목들의 근거 위치 자체는 문제 없음 — 상단 요약 문구의 "451행" 표기만 450행으로 정정 필요.
