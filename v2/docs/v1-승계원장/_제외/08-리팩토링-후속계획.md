---
원본: v1/REFACTOR-NEXT.md (전체, 49행)
정본: 원본 파일이 정본이며 이 원장은 v2 입력용 인벤토리다
---

### 08-1. 문서 성격과 배경 (브랜치 `refactor/sidepanel-decompose`)
- 유형: 교훈·경위
- 출처: REFACTOR-NEXT.md 1~3행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > # 리팩토링 후속 계획 (branch `refactor/sidepanel-decompose`)
  >
  > 야간 자율 작업으로 **foundation 분해까지 완료·검증·커밋**(`9083efc`). 이 문서는 남은 분해의 실행 계획이다. 남은 단계는 **런타임 동작이 바뀔 수 있어 시각/라이브 검증이 필요**하므로 야간 자율로 강행하지 않고 여기에 계획으로 둔다(브랜치는 그대로, 사용자가 5180에서 확인하며 이어가면 됨).

### 08-2. 완료된 foundation 분해 내역
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "완료(이 브랜치, 검증됨)" 5~7행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > ## 완료(이 브랜치, 검증됨)
  > App.tsx(904→785줄)에서 순수 모듈 분리(코드 이동만, 로직 무변경):
  > - `types.ts`(Screen·UITerm·Q·State·Action) · `constants.ts`(MIN_Q·THINK_KEYS·HIGHRISK·LOCALE_TAG·GALAXY_POS) · `text.tsx`(sentLines·splitSentences·firstSentence·fmtDate·markTerms·commaLines) · `file.ts`(isTextFile·readTextFile) · `icons.tsx`(13 아이콘). `mock.ts`의 HIGHRISK도 constants로 단일화.

### 08-3. foundation 분해 검증 방법
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "완료(이 브랜치, 검증됨)" 8행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - 검증: 확장 tsc 클린 + vite build + 이식성 가드 PASS. theme.css·i18n·계약 무변경.

### 08-4. 불변 경계 ① 프론트↔백 계약
- 유형: 제약
- 출처: REFACTOR-NEXT.md "불변 경계(모든 후속 단계 공통, 절대 변경 금지)" 10~11행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ## 불변 경계(모든 후속 단계 공통, 절대 변경 금지)
  > - 프론트↔백 계약(`shared/types`·`prompts`·`interfaces`·`pipeline.stub`), 5 엔드포인트 시그니처(`api.ts`).

### 08-5. 불변 경계 ② 이식성 가드
- 유형: 제약
- 출처: REFACTOR-NEXT.md "불변 경계" 12행
- 폼팩터: 재검토(폼팩터 전환으로 의미 변할 수 있음)
- 원문: > - 이식성(`core/`·`shared/` 런타임 누수 0, `npm run guard`).

### 08-6. 불변 경계 ③ theme.css·i18n·localStorage 키
- 유형: 제약
- 출처: REFACTOR-NEXT.md "불변 경계" 13행
- 폼팩터: 재검토(폼팩터 전환으로 의미 변할 수 있음)
- 원문: > - `theme.css`(프로토타입 verbatim) · i18n 키 · localStorage 키(`sidetab:plan/locale/tutorial-seen`, `sidetab:sessions`, `userId`).

### 08-7. 불변 경계 ④ 5화면 + 페이월/거부 동작
- 유형: 제약
- 출처: REFACTOR-NEXT.md "불변 경계" 14행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - 5화면 + paywall/refusal 동작, 스트리밍/Abort, 무료/유료 게이팅, 튜토리얼 1회.

### 08-8. 남은 단계 총괄(위험 낮은 순으로 정렬)
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "남은 단계 (위험 낮은 순)" 16행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > ## 남은 단계 (위험 낮은 순)

### 08-9. 1단계: 화면 컴포넌트 분리 `screens/`
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "1. 화면 컴포넌트 분리 `screens/`" 18~19행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > ### 1. 화면 컴포넌트 분리 `screens/` — 위험 낮음(기계적, tsc로 검증), 효과 큼
  > App.tsx의 화면 함수를 파일로 이동(현재 props·동작 그대로). 각 파일은 `types`·`text`·`icons`·`i18n`(tr)·하위 컴포넌트를 import.

### 08-10. 1단계 대상 파일 목록
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "1. 화면 컴포넌트 분리" 20행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > - `screens/Header.tsx` · `Entry.tsx` · `Narrow.tsx` · `Detail.tsx` · `Card.tsx`(Detail 사용) · `Terms.tsx`(Card 사용) · `Kept.tsx`(Card 사용) · `Paywall.tsx` · `Refusal.tsx` · `Tutorial.tsx`.

### 08-11. 1단계 목표 효과와 주의사항
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "1. 화면 컴포넌트 분리" 21~22행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > - App.tsx는 이들을 import하고 함수 정의 제거 → App.tsx ~300줄로 축소(가장 큰 가시 효과).
  > - 주의: props 시그니처를 그대로 유지(핸들러 타입은 `types.ts`/인라인). 순환참조 없게 Card↔Detail은 같은 파일 또는 Detail을 Card가 import.

### 08-12. 1단계 검증 방법
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "1. 화면 컴포넌트 분리" 23행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - 검증: 확장 tsc 클린 + vite build + **5180에서 5화면 시각 확인**.

### 08-13. 2단계: 커스텀 훅 `hooks/` 분리 개요와 위험
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "2. 커스텀 훅 `hooks/`" 25~26행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 2. 커스텀 훅 `hooks/` — 위험 중(이펙트·클로저 의미 보존이 관건)
  > App 본문의 로직 덩어리를 훅으로. **이펙트 타이밍·의존성 배열·sref(stale closure) 동작을 그대로 보존**해야 함(여기서 회귀가 가장 잘 생김 → 라이브 검증 필수).

### 08-14. 2단계 대상 훅 목록
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "2. 커스텀 훅 `hooks/`" 27~29행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - `useThinkingTick`(Narrow의 4초 로딩 문구 인터벌) · `useAutoGrow`(textarea 높이) — 가장 독립적, 먼저.
  > - `useNarrowingFlow`(startNarrow·advanceNarrow·nextStep·undoStep·refineFromTerms·toggleSel) · `useStreamRecommend`(runRecommend·loadMore·genGroup·buildRecInput·abortRef) · `useDetailModal`(toggleDetail·jumpRelated) · `useKeepingSession`(persist·toggleKeep·openHistory·buildSummary·onCopy·onShare·aiRefine).
  > - `usePersistence`(localStorage 래퍼: plan·locale·tutorial-seen, try/catch 일원화) · `usePlan`(isPro) · `useConfig`(getConfig).

### 08-15. 2단계 검증 방법(라이브 전수 클릭 항목)
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "2. 커스텀 훅 `hooks/`" 30행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > - 검증: 단계마다 5180에서 **해당 플로 전수 클릭**(다중선택·직접입력·어려워요·되돌리기·더보기·그룹·재탐색·상세·담기·복사·AI정리·페이월·튜토리얼·로케일전환) — 헤드리스로는 안 잡히는 회귀가 있으니 라이브 필수.

### 08-16. 3단계: 서비스 레이어 `services/`
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "3. 서비스 레이어 `services/`" 32~33행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 3. 서비스 레이어 `services/` — 위험 중
  > api 호출 오케스트레이션을 UI 사이드이펙트와 분리(narrowService·termService). 핸들러는 글루로. 훅 단계와 함께/직후.

### 08-17. 4단계: Context 도입
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "4. Context 도입" 35~36행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 4. Context 도입 — 위험 중상(렌더·prop-drilling 구조 변경)
  > `StateContext`+`ActionContext`로 화면에 state·merge·핸들러 직주입 폐지. 렌더 결과 동치 확인.

### 08-18. 5단계: 메모/정리·스타일 합치기
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "5. 메모/정리·스타일 합치기" 38~39행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 5. 메모/정리·스타일 합치기 — 위험 낮음
  > `React.memo`(스크린·Card·Detail), 죽은 코드 제거, 반복 정적 인라인스타일만 신규 css(값 동일, theme.css 무변경). 시각 변동 0 확인.

### 08-19. 6단계: 백엔드 소폭 정리(계약·이식성 불변 내)
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "6. 백엔드 소폭" 41~42행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ### 6. 백엔드 소폭(계약·이식성 불변 내) — 위험 낮음
  > `core/pipeline.ts`·`adapters/workers`의 `siteFromUrl` 중복 → `shared/utils` 단일화, 긴 함수/중복 정리(buildLimits 범위검증·rateLimit 미들웨어화 등 계약 무변경분).

### 08-20. 권장 진행 순서 및 최종 신뢰 기준
- 유형: 확정결정
- 출처: REFACTOR-NEXT.md "권장 진행" 44~45행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > ## 권장 진행
  > 1단계(화면) → 라이브 검증 → 커밋. 2단계 이후는 각 단계마다 라이브 검증+커밋(회귀 시 단계 단위 롤백). 모두 끝나면 master 머지. 헤드리스 테스트(jsdom+testing-library)를 먼저 구축하면 1차 안전망이 되나, **최종 신뢰는 5180 시각 확인**.

### 08-21. 참고: App.tsx 현재 구조 맵
- 유형: 근거·수치
- 출처: REFACTOR-NEXT.md "참고 — App.tsx 현재 구조 맵" 47~48행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > ## 참고 — App.tsx 현재 구조 맵
  > App 본문(useReducer+sref+merge, 핸들러 ~30개, useEffect 5개, 렌더) → 화면 함수(Header·Entry·Narrow·Detail·Card·Terms·Kept·Paywall·Refusal·Tutorial) → `msg()`. 핸들러는 위 훅 그룹으로 매핑됨.
