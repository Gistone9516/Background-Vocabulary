---
원본: v1/docs/배경어휘-사이드탭-사용자판단대기.md (전체, 68행)
정본: 원본 파일이 정본이며 이 원장은 v2 입력용 인벤토리다
---

### 07-1. 문서 성격(park-and-continue 원칙)
- 유형: 교훈·경위
- 출처: 배경어휘-사이드탭-사용자판단대기.md 1~4행(표제/서문)
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > # 사용자 판단 대기 (parked) — 2026-06-25 자율 빌드 세션(cycle 2 프론트 이식)
  >
  > buildflow park-and-continue 원칙: 자율로 결정하지 않고 사용자 확인이 필요한 항목만 모았다.
  > cycle 2(프론트 이식)는 완료·검증됨(아래 "완료" 참조). 아래는 현재 동작을 막지 않는, 다음에 정할 것들.

### 07-2. cycle 1 완료 사항(백엔드 계약)
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-사용자판단대기.md "완료 (이번 세션)" 7행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > cycle 1: 백엔드 계약 반영(타입체크+이식성 가드+실 API e2e PASS). 커밋 `cycle1-backend`.

### 07-3. cycle 2 완료 사항(프론트 이식)
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-사용자판단대기.md "완료 (이번 세션)" 8행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > cycle 2: panel.html 5화면을 React 확장으로 이식 + 5엔드포인트 배선(DEV=mock / 빌드=실 워커). vite 빌드 PASS, 헤드리스 렌더 검증 통과(entry→narrow→terms→detail 출처→summary, 콘솔 에러 0).

### 07-4. detail 출처 영어화 여부 [품질]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "1. detail 출처 영어화 [품질]" 12~14행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 현재: 어휘명이 한국어("적분기 와인드업")라 그 단어로 검색하면 **한국어 저품질 블로그**(tistory/naver)가 출처로 잡힘. 영어 RAG 철학(고품질 영어 출처)과 어긋남.
  > 옵션: detail 검색 쿼리를 영어 용어/분야로 구성, 또는 recommend의 영어 RAG 캐시를 구조화 저장해 재사용. 어느 방향?

### 07-5. WORKER_BASE 실 도메인 미확정 [배포]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "2. WORKER_BASE 실 도메인 [배포]" 16~18행
- 폼팩터: 재검토(폼팩터 전환으로 의미 변할 수 있음)
- 원문: > 현재 `api.ts`의 WORKER_BASE = `sidetab-api.example.workers.dev`(placeholder). 빌드(프로덕션)는 이 주소로 fetch하므로 실제로 동작하려면 배포된 워커 URL 필요. DEV(vite dev)는 mock이라 무관.
  > 로컬 검증 단계: `wrangler dev`(보통 localhost:8787) + WORKER_BASE를 로컬로 + manifest host_permissions에 localhost 추가가 필요.

### 07-6. Pretendard 폰트 self-host 필요 여부 [MV3 CSP]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "3. Pretendard 폰트 self-host [MV3 CSP]" 20~21행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > theme.css가 panel.html처럼 CDN(jsdelivr)에서 Pretendard를 불러옴. MV3 CSP가 외부 CDN 폰트를 막을 수 있음 → 프로덕션은 폰트를 확장에 포함(self-host)해야 안전. 지금은 차단 시 시스템 폰트로 폴백(렌더 자체는 정상).

### 07-7. 진입 context_object(붙여넣은 문서) 입력 필드 [UX]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "4. 진입 context_object(붙여넣은 문서) 입력 [UX]" 23~24행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 현재 진입 컴포저는 입력 한 칸 + "조건 추가"(user_condition)뿐. "막힌 문서를 붙여넣기"용 별도 context_object 필드는 없음(원문 입력이 topic으로 감). 문서 붙여넣기 UX를 따로 둘지?

### 07-8. "AI로 더 정리" 무료 클릭 동작 확인 [UX]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "5. "AI로 더 정리" 무료 클릭 동작 [UX 확인]" 26~27행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 무료에서 누르면 페이월로 보냄(유료 유도). 버튼 문구 "(pro 전용 🔒)". 이 동작/문구 OK인지.

### 07-9. 자율 결정(FYI, 비차단) 4건
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-사용자판단대기.md "자율 결정(FYI, 비차단)" 29~33행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > React 상태 = useReducer + stateRef(panel.html 전역 S 패턴 충실 이식). 화면=컴포넌트, theme.css=panel.html 스타일 verbatim.
  > 무료 정리문 조사 오류 수정: "활용를"→"활용을"(받침 판별 헬퍼 eul()).
  > 죽은 파일 제거: TermCard.tsx, mockApi.ts(각각 인라인 Card, mock.ts로 대체).
  > DEV mock은 대표 어휘 3종(축약). 실 데이터는 실 API 경로.

### 07-10. 보안·비용 감사 보류 섹션 배경(Tier3 로드맵)
- 유형: 교훈·경위
- 출처: 배경어휘-사이드탭-사용자판단대기.md "보안·비용 감사 보류(Tier3 로드맵) — 2026-06-28 A-Z 전수 감사" 37~39행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 6렌즈 적대 검증(보안·상태·에러·계약·비용·UX) 확정 33건 중, 명확한 기능 버그와 싼 비용 백스톱은 이번에 수정했다. 아래는 **진짜 인증·결제 인프라(Tier3)가 필요해 애드혹 패치 시 정상 사용까지 깨질 위험**이라 보류한 항목과, 한 건의 결정 사항이다. 코드 자체가 이미 "설치 UUID 바인딩 Tier3 전까지 구조적 잔존"으로 명시한 영역이다.

### 07-11. B1. 익명 주간 미터링 우회 [Tier3]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "B1. 익명 주간 미터링 우회 [Tier3]" 41~44행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 현재: `x-user-id` 헤더를 생략하면 `metered=false`라 주간 카운터를 타지 않는다(`index.ts` classify). 익명·비브라우저 호출이 per-user 한도를 통째로 비껴간다.
  > 임시 방어: 전역 일일 캡(이번 감사로 `/next`·`/preview`·`/relate`에도 확장)과 IP 레이트리밋.
  > 옵션: ① 익명에 IP 기반 주간 카운터 ② 설치 UUID 서버 바인딩(Tier3, 결제와 함께). 권장 = ②.

### 07-12. B2. x-tier 헤더 미검증(누구나 paid 선언) [Tier3]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "B2. x-tier 헤더 미검증(누구나 paid 선언) [Tier3]" 46~49행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 현재: `readTier`가 클라가 보낸 `x-tier`를 그대로 믿는다. free 사용자가 `x-tier: paid`로 높은 토큰 상한과 무제한 detail을 받는다.
  > 임시 방어: 전역 일일 캡 + IP 레이트리밋(비용 총량은 막힘, 티어 구분만 우회 가능).
  > 옵션: 서버 결제 검증(Tier3). 그 전까지는 무료 모델 BM이라 손해가 토큰 단가 차이로 제한적.

### 07-13. B3. detail 무료 횟수 서버 카운터 없음 [Tier3·모니터링]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "B3. detail 무료 횟수 서버 카운터 없음 [Tier3·모니터링]" 51~54행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 현재: `detailLimitFree`(3회) 검사가 클라 메모리에만 있다. 새로고침이나 직접 호출로 무력화 가능(detail은 Tavily+LLM 이중 비용).
  > 임시 방어: 클라 가드 + 전역 일일 캡 + IP 일 200회.
  > 옵션: IP+day detail 전용 카운터. 비용 실측 후 필요하면 추가.

### 07-14. B4. CORS origin=null 와일드카드 [배포 시 잠금]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "B4. CORS origin=null 와일드카드 [배포 시 잠금]" 56~58행
- 폼팩터: 크롬확장-종속(v2에서 재설계 필요)
- 원문: > 현재: Origin 헤더 없는 요청(curl 등)에 `*` 반환. 비브라우저 호출이 무제한 접근.
  > 권장: 프로덕션 배포 때 `ALLOWED_EXT_ORIGIN`(확장 ID) 설정 + 개발 외 no-Origin 거부. 배포 가이드에 반영.

### 07-15. B5. 고위험 게이트의 client domain_risk 신뢰(매핑 미스 도메인) [저위험·모니터링]
- 유형: 보류·미해결
- 출처: 배경어휘-사이드탭-사용자판단대기.md "B5. 고위험 게이트의 client domain_risk 신뢰(매핑 미스 도메인) [저위험·모니터링]" 60~63행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 현재: 정적 도메인맵에 안 잡히는("other") 도메인은 클라가 보낸 `domain_risk`를 그대로 라우팅에 쓴다. 의료·법률처럼 들리지만 키워드를 피한 입력에 `domain_risk: low`를 실으면 고위험 거부를 우회.
  > 완화: 정적 키워드맵(의료·진단·medical·법률·legal 등)이 1차 방어이고 서버측이라 우회 불가. 우회의 결과도 "어휘 추천"이라 피해 상한이 낮음.
  > 옵션: 미매핑 도메인 서버 재분류 또는 추가 키워드 스크린. 모니터링하다 필요 시.

### 07-16. 결정(보류 아님): 전역 캡 fail-open 유지
- 유형: 확정결정
- 출처: 배경어휘-사이드탭-사용자판단대기.md "결정(보류 아님) — 전역 캡 fail-open 유지" 65~67행
- 폼팩터: 무관(v2 그대로 유효)
- 원문: > 항목: `bumpGlobalCap`·`rateLimited`가 Upstash 오류 시 통과(fail-open)라, Upstash 장애 동안 캡이 무력.
  > 결정: **fail-open 유지**(fail-closed 미적용). 이유 = fail-closed는 Upstash 일시 장애(네트워크 blip)에 전 사용자 서비스를 중단시킨다(가용성 우선 설계 의도). CF 워커는 isolate가 휘발이라 in-memory fallback 카운터도 사실상 무효. 주요 비용 구멍(미집계 LLM 엔드포인트)은 이번에 닫았으므로 실효 위험이 낮다. 재검토 = 배포 후 비용·장애 빈도 실측 시.

## 검증 보충
- 검증 방법: 원본 v1/docs/배경어휘-사이드탭-사용자판단대기.md 전체(68행)를 Read로 재독하여 원장 07-1~07-16과 줄 단위로 전수 대조.
- 누락: 없음. 원본의 표제/서문, cycle1·cycle2 완료 사항, 결정 대기 5항목(detail 영어화, WORKER_BASE, Pretendard, context_object, AI 정리 클릭), 자율 결정 4건, Tier3 보안 감사 보류 B1~B5, fail-open 결정까지 전 항목이 07-1~07-16에 존재함을 확인.
- 요약화(의역·축약): 없음. 모든 항목의 "원문" 인용이 원본과 글자 단위로 일치(강조 표기 `**...**`, 코드 표기 `` ` ``, 특수문자 포함).
