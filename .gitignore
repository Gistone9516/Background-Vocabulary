# secrets — never commit
.env
.env.*
*.local
.dev.vars

# deps / build
node_modules/
dist/
.wrangler/
*.tsbuildinfo

# os / editor
.DS_Store
Thumbs.db

# 도구 프로파일 · 과거 산출물(v1 내부는 v1/.gitignore가 관리)
.playwright-mcp/
