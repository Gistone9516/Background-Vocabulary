# 사용자 판단 대기 (parked) — 2026-06-25 자율 빌드 세션(cycle 2 프론트 이식)

> buildflow park-and-continue 원칙: 자율로 결정하지 않고 사용자 확인이 필요한 항목만 모았다.
> cycle 2(프론트 이식)는 완료·검증됨(아래 "완료" 참조). 아래는 현재 동작을 막지 않는, 다음에 정할 것들.

## 완료 (이번 세션)
- cycle 1: 백엔드 계약 반영(타입체크+이식성 가드+실 API e2e PASS). 커밋 `cycle1-backend`.
- cycle 2: panel.html 5화면을 React 확장으로 이식 + 5엔드포인트 배선(DEV=mock / 빌드=실 워커). vite 빌드 PASS, 헤드리스 렌더 검증 통과(entry→narrow→terms→detail 출처→summary, 콘솔 에러 0).

## 결정 대기 항목

### 1. detail 출처 영어화 [품질]
- 현재: 어휘명이 한국어("적분기 와인드업")라 그 단어로 검색하면 **한국어 저품질 블로그**(tistory/naver)가 출처로 잡힘. 영어 RAG 철학(고품질 영어 출처)과 어긋남.
- 옵션: detail 검색 쿼리를 영어 용어/분야로 구성, 또는 recommend의 영어 RAG 캐시를 구조화 저장해 재사용. 어느 방향?

### 2. WORKER_BASE 실 도메인 [배포]
- 현재 `api.ts`의 WORKER_BASE = `sidetab-api.example.workers.dev`(placeholder). 빌드(프로덕션)는 이 주소로 fetch하므로 실제로 동작하려면 배포된 워커 URL 필요. DEV(vite dev)는 mock이라 무관.
- 로컬 검증 단계: `wrangler dev`(보통 localhost:8787) + WORKER_BASE를 로컬로 + manifest host_permissions에 localhost 추가가 필요.

### 3. Pretendard 폰트 self-host [MV3 CSP]
- theme.css가 panel.html처럼 CDN(jsdelivr)에서 Pretendard를 불러옴. MV3 CSP가 외부 CDN 폰트를 막을 수 있음 → 프로덕션은 폰트를 확장에 포함(self-host)해야 안전. 지금은 차단 시 시스템 폰트로 폴백(렌더 자체는 정상).

### 4. 진입 context_object(붙여넣은 문서) 입력 [UX]
- 현재 진입 컴포저는 입력 한 칸 + "조건 추가"(user_condition)뿐. "막힌 문서를 붙여넣기"용 별도 context_object 필드는 없음(원문 입력이 topic으로 감). 문서 붙여넣기 UX를 따로 둘지?

### 5. "AI로 더 정리" 무료 클릭 동작 [UX 확인]
- 무료에서 누르면 페이월로 보냄(유료 유도). 버튼 문구 "(pro 전용 🔒)". 이 동작/문구 OK인지.

## 자율 결정(FYI, 비차단)
- React 상태 = useReducer + stateRef(panel.html 전역 S 패턴 충실 이식). 화면=컴포넌트, theme.css=panel.html 스타일 verbatim.
- 무료 정리문 조사 오류 수정: "활용를"→"활용을"(받침 판별 헬퍼 eul()).
- 죽은 파일 제거: TermCard.tsx, mockApi.ts(각각 인라인 Card, mock.ts로 대체).
- DEV mock은 대표 어휘 3종(축약). 실 데이터는 실 API 경로.
