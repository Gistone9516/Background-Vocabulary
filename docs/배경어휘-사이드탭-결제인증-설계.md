# 배경노트 — 결제·인증·엔타이틀먼트 설계 (v1, 웹근거)

> 목표 = pro(구독) 결제·인증·엔타이틀먼트를 **전부 구현**하되 출시 시 결제는 피처플래그로 **비활성** 가능. 미구현 채로는 출시하지 않는다.
> 방법 = 백그라운드 Workflow로 결제사·인증·영속 6주제 웹조사(출처 포함) + 아키텍처 6렌즈(비용·보안·이식성·운영·UX·법규) 적대 평가 2라운드. opus 종합.
> 근거 강도 표기: [H]=다출처 확인, [M]=단일/추정 혼재, [확인필요]=미확정. 결정을 가르는 사실은 본문에 출처 URL.

---

## 0. 한 줄 결론

**PG부터 고르지 말고 "포트(인터페이스) + 선행 인프라"부터 구현한다.** 6렌즈가 R2에서 만장일치로 수렴한 것은 PG 선택이 아니라 그 **앞에 와야 할 4가지**다: ① `PaymentGatewayPort` 추상화 ② Cloudflare D1 엔타이틀먼트 SoT ③ 서버 발급 서명 JWT(현재 `x-tier` 헤더 신뢰 구멍 제거) ④ 한국법 유료전환 고지 체계. PG(토스·포트원·Paddle)는 이 포트 뒤에 어댑터로 꽂으며, 어느 것을 켜느냐는 **가격·타깃 결정**에 종속된다(§B).

> **결정 확정(2026-06-29, 사용자):** PG = **포트원 단일 SDK**(국내 토스페이먼츠 빌링 + 해외 페이레터/엑심베이 분기, 사용자 원안 "이중등록"). 가격 = **국내 월 3,900원 / 해외 월 $2.99(세금 제외)**. Paddle(소액 실효 ~25% 적자)·Stripe(한국 법인 불가, 2026-06)는 보류. 따라서 §B는 포트원 경로로 확정, 착수 대상은 Phase 0(§A·§D).

---

## 1. 맥락과 현재 코드의 구멍 (실측)

- 스택: 크롬 MV3 사이드패널 확장 + Cloudflare Workers(stateless) + Upstash Redis. 1인 개발. pro 월 3,500원(원화).
- 현재 pro는 **localStorage 토글**일 뿐 실제 결제 없음(`onUpgrade` = `localStorage.setItem("sidetab:plan","pro")`).
- **렌즈가 실제 코드에서 확인한 구조적 취약점:**
  - `workers/src/index.ts`의 `readTier()`가 요청 헤더 `x-tier`가 `paid`면 그대로 신뢰(서버에 엔타이틀먼트 SoT 없음). DevTools에서 헤더만 바꾸면 pro 기능 즉시 개방.
  - `/summarize`·`/detail`은 tier만 보고 그 tier 출처를 보증하지 않음. `x-user-id`도 자기신고라 매 요청 새 UUID로 보내면 주간 한도 무한 우회(코드 주석이 "Tier3 전까지 구조적 잔존"으로 자인).
  - `detailLimitFree`(무료 상세 3회)는 클라 state로만 집계 → 새로고침·직접호출로 무력화.
  - 전역 일일 캡·레이트리밋이 `fail-open`(Upstash 장애 시 통과)이라 유일 backstop이 단일 외부 의존.
- 결론: **결제를 "켜는 순간" 이 구멍들이 그대로 드러난다.** 그래서 결제 인프라보다 서버 엔타이틀먼트 검증이 먼저다(보안·이식성·운영·UX·법규 렌즈 R2 만장일치).

---

## 2. 결제사 웹조사 요약 (출처)

| 후보 | 핵심 | 수수료(월 3,500원 기준) | 한국 1인 가능? | 결정적 제약 |
|---|---|---|---|---|
| **토스페이먼츠** | 빌링키 자동결제 정식 지원 | 카드 4.3%+VAT(영중소 우대 가능). 건당 약 150원, 실효 ~4.3% | O(개인사업자+통신판매업) | 빌링 **별도 심사 10~14영업일**, **스케줄러 자체 구현 필수**, **국내카드 전용** |
| **PortOne(포트원)** | 단일 SDK로 국내+해외 PG 분기(=사용자의 "이중등록"에 정합) | 중개 0.5% + 각 PG 수수료. **월 5천만원 이하 플랫폼료 0원** | O(개인사업자, 토스 등 선택) | Stripe 직연동 미확인. 해외PG(페이레터·엑심베이) 수수료는 상담 필요 |
| **Stripe** | kr-card+Kakao/Naver/PAYCO 기술 지원 | ~3.1%+30¢ | **불가**(2026-06 한국 법인 미지원) | 해외법인(싱가포르 등) 필요, KRW 정산 불가, **DCC 이중환전 미해결** |
| **MoR(Paddle·Lemon Squeezy·Polar)** | VAT/판매세 전세계 대행, 법인 불요 | 5%+$0.50 → **실효 ~25~27%** | O(개인, Paddle은 카카오/네이버페이 공식) | KRW 직접정산 불가(USD 수령 후 환전). 소액 구독에서 $0.50 정액이 치명 |

출처: 토스 빌링 https://docs.tosspayments.com/guides/v2/billing · 수수료 https://www.tosspayments.com/about/fee · 빌링심사 https://techchat.tosspayments.com/m/1377644939881484331 / 포트원 https://www.portone.io/one-payment-infra · 슈퍼빌링키 https://blog.portone.io/super_billing_key/ · 요금 https://help.portone.io/content/portone-pricing / Stripe KR https://docs.stripe.com/billing/subscriptions/kr-card · 한국법인 불가 정리 https://blog.portone.io/opi_globalpayment-krpg/ · DCC https://blog.popekim.com/en/2025/11/02/stripe-krw-local-payment-dcc.html / Paddle 세무대행 https://www.paddle.com/help/start/intro-to-paddle/how-paddle-is-able-to-take-on-your-vat-and-tax-responsibilities · 한국결제수단 https://developer.paddle.com/changelog/2025/improved-korean-payment-methods · 수수료 https://www.paddle.com/pricing

**소액 구독의 진실(렌즈 합의):** 월 3,500원에서 Paddle 고정비 $0.50은 거래액의 20%를 단독 점유 → 실효 ~25%. 세무대행은 EU VAT·미국 판매세가 **실제로 발생할 때만** 가치가 있는데 초기 국내 서비스엔 미발생 → 비용만 남음. 단가를 5,000~9,900원/연간 플랜으로 올리면 실효 ~10% 내외로 하락.

---

## 3. 인증·영속 웹조사 요약 (출처)

- **인증 = `chrome.identity` getAuthToken(Google OAuth), Google `sub`를 사용자 PK.** 마찰 0, 토큰 캐싱 자동(MV3 서비스워커 지원). UUID 디바이스 바인딩은 결제 연동 시 재설치·교차기기·환불 대응 불가 → **보조(미인증 세션 추적)로만**. (Brave 등 비크롬 지원 필요 시 `launchWebAuthFlow` 대안.) https://developer.chrome.com/docs/extensions/reference/api/identity
- **엔타이틀먼트 SoT = Cloudflare D1(SQLite).** Upstash/KV는 쓰기 전파 ~60초 eventual consistency → webhook 직후 tier 판별 오염. D1은 SQL 트랜잭션 원자 upsert + 프리 150M reads·3M writes·5GB(저트래픽 비용 ~0). https://developers.cloudflare.com/d1/platform/pricing/ · 저장소 비교 https://developers.cloudflare.com/workers/platform/storage-options/
- **Upstash = 역할 축소(세션 캐시 + rate-limit + RAG 캐시 전용).** SoT 아님.
- **webhook → D1 atomic upsert(멱등).** Stripe `entitlements.active_entitlement_summary.updated` 또는 PG별 이벤트, `Stripe-Signature` HMAC 검증(`constructEventAsync`, Workers `node_compat=true`), `wrangler secret`로 시크릿 주입. https://docs.stripe.com/billing/entitlements · https://hono.dev/examples/stripe-webhook
- **서버 발급 서명 JWT로 `x-tier` 제거.** 클라가 Google ID 토큰 제출 → Workers가 Google tokeninfo로 **최초 1회** 검증 → D1 tier 조회 → `{sub,tier,exp}` JWT(HS256, exp 1h) 발급 → 이후 모든 요청은 **JWT 서명만 검증**(per-request 외부호출·D1 재조회 없음, UX 렌즈 지적 반영). 취소 즉시 반영은 짧은 exp + 갱신 엔드포인트(+ 필요시 Upstash 블랙리스트).
- CWS: 자체결제 폐지(2021), **외부결제 허용**. 확장 내 결제폼 금지 → `chrome.tabs.create`로 외부 hosted checkout. https://developer.chrome.com/docs/webstore/program-policies/accepting-payment/

---

## A. 확정 사항 (PG와 독립, "전부 구현"의 뼈대 — 렌즈 만장일치)

이 8개는 어떤 PG를 켜든 동일하게 필요하다. **결제 비활성 출시여도 이 인프라는 동작**해야 "구현 완료"다.

- **A1 `PaymentGatewayPort` 먼저.** `adapters/workers/src/payment/port.ts`에 `createCheckout`·`verifyWebhook`·`cancel`·`refund` 인터페이스. 토스/포트원/Paddle은 각각 어댑터 1파일. PG 전환=포트 교체. 이식성 렌즈 결론: "스택을 먼저 고르지 말고 포트를 먼저 정의하라."
- **A2 인증.** `chrome.identity` Google OAuth, `google_sub` = 사용자 PK. UUID는 미인증 세션 보조.
- **A3 엔타이틀먼트 SoT = D1 + `UserRepository` 추상화.** `users{ google_sub PK, tier, subscription_status, expires_at, pg_provider }`(컬럼을 처음부터 둬서 후일 PG 추가 시 마이그레이션 0). `adapters/workers/src/db/d1-user-repo.ts`에만 D1 바인딩. **core/shared D1 참조 0 확인됨**(이식성 불변 유지, Lambda 이전 시 `DynamoDBUserRepository`로 교체).
- **A4 서버 서명 JWT.** `readTier()`를 JWT claims에서 읽도록 교체. 유료 엔드포인트(`/summarize`·`/detail`·`/recommend`·`/relate`·`/next` 등) JWT 검증.
- **A5 webhook 수신.** HMAC 서명 검증 → D1 atomic upsert, 멱등(중복 수신 대비). 시크릿은 `wrangler secret`.
- **A6 서버측 미터링(Tier3 B1·B3 봉합).** 주간 한도·detail 횟수를 D1/Upstash 서버 카운터로(클라 신뢰 제거). `google_sub` 기준이라 헤더 교체 우회 불가.
- **A7 한국법 유료전환 고지 체계.** 무료→유료 전환·가격 인상 시 **30일 전 고지 + 7일 전 리마인드**(전자상거래법·소비자기본법). 이메일 발송 + 동의 기록 DB. **PG와 무관하게 필수** — 없으면 피처플래그를 켤 수 없다. https://docs.stripe.com/billing/subscriptions/kr-card
- **A8 피처플래그 `PAYMENT_ENABLED`.** Workers env. false=전원 pro 처리 + checkout 라우트 stub(또는 coming soon). true 전환의 **하드플로어 = A1~A7 완료 + 고지 30일 경과**.

---

## B. PG 선택 (사용자 결정 — 가치/우선순위 분쟁, fact 분쟁 아님)

> **확정: 포트원 단일 SDK(국내 토스 빌링 + 해외 페이레터/엑심베이), 가격 국내 월 3,900원 / 해외 월 $2.99(세금 제외).** 아래는 결정 배경과 보존할 소수의견.

렌즈는 여기서 갈렸고, **이건 fact가 아니라 사용자의 가치 선택**이다(=납작하게 만들지 않고 사용자가 결정).

- **축1 가격:** 3,500원 유지 → 토스/포트원만 흑자(Paddle은 ~25%로 적자). 5,000원+·연간 플랜 → Paddle도 ~10%대 가능.
- **축2 타깃:** 국내 우선 → 토스 단독으로 출발. 즉시 글로벌(다국어 ko/en/ja/zh 매출 천장 회피) → 해외 결제수단 필요.

**사용자의 명시 의도("국내 한국 PG + 해외 글로벌 PG 이중등록")에 가장 정합하는 경로:**
- **포트원 단일 SDK** = 국내(토스페이먼츠 빌링) + 해외(페이레터 글로벌/엑심베이) 분기를 **한 연동으로**. 월 5천만원 이하 플랫폼료 0원이라 초기 비용은 PG 수수료만(국내 토스 ~4.3% + 포트원 0.5%). 이중등록이 PortOne 하나로 성립(웹조사 확인). [M — 해외PG 수수료·소액 최소금액은 상담/확인필요]

**렌즈 권장(다수): 토스/포트원 우선, Paddle은 조건부.**
- 비용·보안·이식성·UX 렌즈: 3,500원 유지면 토스(포트원 경유)로 출발, Stripe는 한국법인 열릴 때까지 보류, 글로벌은 포트원 어댑터 추가.

**소수의견(보존 — ops·legal 렌즈, R2에서도 유지):** **Paddle 단독 MoR.** 근거 = 세무(VAT/판매세) 전세계 대행 + 법인 불요 + 구독 수명주기(갱신·취소·재시도) Paddle 관리라 1인이 스케줄러를 직접 안 짬 → 6개월 운영 붕괴 리스크 최소. **단 전제: 단가를 5,000원+/연간으로 올려 수수료 실효 20%↓.** 반론(다수): MoR도 A1~A7(특히 D1+JWT·고지)은 그대로 필요해 "운영 간소화" 실질 절감폭은 과장. 그리고 Paddle은 영국법인이라 국내 분쟁조정 관할 밖(집행 불확실).

---

## C. 구독 수명주기 (포트 뒤 어댑터가 흡수)

- 신규: checkout 성공 webhook → D1 `subscription_status=active, expires_at`.
- 갱신/실패: 토스 직결이면 **스케줄러 자체 구현**(Cloudflare Cron Trigger 또는 Upstash QStash) + 카드 만료 재시도 + grace. Paddle/포트원-해외PG는 플랫폼이 일부 관리. (이 부담 차이가 §B 소수의견의 핵심.)
- 취소/환불: 빌링키 삭제 또는 PG cancel/refund API. 청약철회 14일(국내) 고려.

---

## D. 단계별 구현 계획

- **Phase 0 (PG 무관 선행 — 지금 착수):** A1 포트 → A2 인증 → A3 D1+UserRepository → A4 서버 JWT(`readTier` 교체) → A5 webhook → A6 서버 미터링 → A7 고지 체계 → A8 플래그. 결제 OFF로 main 머지 가능(전원 pro). **리팩토링 브랜치와 충돌 피하려 결제 라우트는 OFF로만 진입.**
- **Phase 1 (어댑터):** 결정된 PG 어댑터 1개 + (병행) 사업자등록·통신판매업·토스 빌링 심사 신청. Paddle 갈 거면 Paddle 어댑터.
- **Phase 2 (활성화):** A1~A7 완료 + 고지 30일 경과 확인 → `PAYMENT_ENABLED=true`.

---

## E. Tier3 보안 연계 (이 설계로 닫히는 항목)

[사용자판단대기](배경어휘-사이드탭-사용자판단대기.md) §B의 보류 항목이 A로 해소: **B2 x-tier 미검증**=A4 JWT, **B1 익명 미터링 우회**·**B3 detail 서버카운터**=A6, **B5 고위험 클라 신뢰**=서버 재검증(별건). **B4 CORS origin 잠금**=배포 단계(`ALLOWED_EXT_ORIGIN`). fail-open 전역캡은 기존 결정 유지(가용성 우선).

---

## F. 미확정·확인 필요 (flip 조건)

- **[확인필요] 토스 빌링 심사를 "코드 완성 전" 미리 신청 가능한가** — cost 렌즈는 "심사는 사업자등록+계획서를 본다"며 병행 가능 주장(미확정). 사실이면 14일 허들이 블로커가 아니라 병렬 경로. **토스에 직접 확인 권장.** 결과가 PG **타이밍**을 가르나, A(선행 인프라)에는 영향 없음.
- **[flip] Stripe 한국 법인 지원 개통** 시 → kr-card 내장+Stripe Tax로 Stripe 단일화 재검토(수수료·세무 우위).
- **[flip] 해외 사용자 비율 ≥30% 확인** 시 → 포트원 이중/Paddle 정당화.
- **[확정] 가격: 국내 월 3,900원 / 해외 월 $2.99(세금 제외)(2026-06-29, 3,500→3,900 수수료 고려 상향)** — Paddle 경로는 닫힘(소액 실효 ~25%). 국내=토스(~4.3% + 포트원 0.5%), 해외=엑심베이 등 USD 외화 정산. **[법규 주의] 해외 디지털 서비스 부가세는 비MoR(포트원 직연동)이라 판매자 책임** — EU OSS 등 일정 매출 이상에서 등록·신고 의무 발생 가능(§E). "세금 제외" 표기는 list price 기준이며, 과세 관할에서는 체크아웃에서 세금 가산 또는 추후 등록 필요. 비용수익 예측(`docs/비용수익_예측.md`) 재확인 권장.
- **[확인필요] 포트원 해외PG(페이레터·엑심베이) 수수료·소액 최소결제금액**(일부 PG 5,000원 미만 제한 가능) — 상담 확인.

---

## H. 실 결제 사용자 여정 (미리보기 제거 — 2026-06-29 사용자 결정)

**미리보기 토글 제거 확정.** localStorage plan-flip 방식의 "pro 미리보기"를 없애고, 페이월 CTA를 실 결제 진입으로 바꾼다.

**사용자 여정(실 결제):** 페이월 "Pro 구독하기" → ① **계정 연결**(`chrome.identity` Google OAuth, `google_sub`) → ② **외부 탭 포트원 hosted checkout**(결제 정보 입력 — 국내 토스 빌링 / 해외 페이레터·엑심베이, 지역 분기) → ③ 결제 완료 → 포트원 **webhook** → Workers HMAC 검증 → **D1 엔타이틀먼트 upsert** → ④ 확장이 폴링(3초 간격, 최대 5분) 또는 storage 이벤트로 pro 전환 감지 → 자동 pro UI + "pro 활성화" 토스트.

**가드(레퍼런스 반영):** 팝업차단 폴백 링크("탭이 안 열리면 여기") · 이미 pro 복구(이메일 로그인 링크, 재설치·기기 이전 대응) · CTA 직하 신뢰 1줄("언제든 해지 · 안전한 외부 결제로 이동") · 다크패턴 회피(confirm-shaming·가짜 배지·강제 연간 금지).

**게이팅·현 상태:** `PAYMENT_ENABLED=false`면 CTA는 "준비 중/출시 전"(현재 상태). `true`면 실 checkout. **미리보기 토글은 제거**(개발 중 pro 테스트는 devtools `localStorage sidetab:plan='pro'` 또는 플래그 off로 전원 pro). 페이월 표현(지역 가격 ₩3,900/$2.99·인라인 추천 pill·가격 17px·가치 문구)은 이미 적용됨. 실 결제 동작만 Phase 0 코드로 연결.

---

## I. 1만명 가정 운용 (DB · 용량 · 비용)

**전제(가정):** 누적 1만 사용자, pro 전환 약 4.3%(`docs/비용수익_예측.md` BEP) ≈ 약 430 pro. [추정]

**DB = Cloudflare D1 도입(§A3 확정의 실체화).** "이제 DB를 쓴다" = Phase 0에서 D1 실제 사용. 테이블: `users{ google_sub PK, tier, subscription_status, expires_at, pg_provider, created_at }`, `payment_events{ event_id PK, ... }`(webhook 멱등 보장), `consents{ user, type, ts }`(30일 고지 동의 기록).

**용량(웹근거 수치, 1만명 기준 여유 큼):** D1 free = 150M reads · 3M writes · 5GB/월. 엔타이틀먼트는 JWT(exp 1h)라 **요청마다 DB read가 아님** → read ≈ 활성자 × (로그인 + 시간당 갱신). 1만 전원 일일 활성·시간당 갱신을 가정해도 1만 × 24 × 30 ≈ 7.2M reads/월 ≪ 150M. writes는 결제·갱신·취소 시만 ≈ 월 수백 ≪ 3M. 1만 행 ≪ 5GB. → **D1 free tier로 1만명 충분**(Workers Paid $5/월은 LLM 프록시로 이미 필요). 출처: developers.cloudflare.com/d1/platform/pricing.

**나머지 인프라:** 포트원 = 월 거래액 5천만원 이하 플랫폼료 0원(430 × 3,900 ≈ 168만원 ≪ 5천만 → PG 수수료만 발생). Upstash = 세션 캐시·rate-limit 전용, 1만 트래픽이면 free 500K commands/월 초과 가능 → pay-as-you-go($0.20/100K, 소액).

**지배 비용 = LLM(DeepSeek)**, 결제·DB 인프라는 미미(`비용수익_예측`과 정합). 1만명 대비 **전역 일일 캡·레이트리밋 env를 상향**(코드 무수정, 워커 env·`/config`). 확장(1만→10만)은 D1 Paid(25B reads/50M writes)로 선형 대응, 또는 Lambda 이전 시 `UserRepository` 뒤에서 DynamoDB 어댑터 교체(이식성 불변).

**외부 준비물(사용자 직접):** 포트원 가입 + 국내 PG(토스) 계약(개인사업자 + 통신판매업) · 해외 PG(페이레터/엑심베이) 계약 · Google OAuth 클라이언트(`chrome.identity`) · `wrangler secret`(포트원 키·webhook 시크릿·JWT 서명키) · 개인정보처리방침·이용약관 페이지. 코드(D1·포트·JWT·OAuth 검증·webhook·checkout·페이월 CTA)는 내가, 자격증명·계약·법무 문서는 사용자가.

---

## J. 출처 (load-bearing)

토스: docs.tosspayments.com/guides/v2/billing, /cancel-payment, tosspayments.com/about/fee, techchat.tosspayments.com/m/1377644939881484331 · 포트원: portone.io/one-payment-infra, blog.portone.io/super_billing_key/, help.portone.io/content/portone-pricing, blog.portone.io/opi_globalpayment-krpg/ · Stripe: docs.stripe.com/billing/subscriptions/kr-card, /billing/entitlements, /payouts/multicurrency-settlement, blog.popekim.com/en/2025/11/02/stripe-krw-local-payment-dcc.html · MoR: paddle.com/pricing, paddle.com/help/.../how-paddle-is-able-to-take-on-your-vat-and-tax-responsibilities, developer.paddle.com/changelog/2025/improved-korean-payment-methods, docs.lemonsqueezy.com/help/getting-started/fees, polar.sh/docs/merchant-of-record/supported-countries · CWS: developer.chrome.com/docs/webstore/program-policies/accepting-payment/ · 인증·영속: developer.chrome.com/docs/extensions/reference/api/identity, developers.cloudflare.com/d1/platform/pricing/, developers.cloudflare.com/workers/platform/storage-options/, hono.dev/examples/stripe-webhook

> 본 문서는 설계안(코드 변경 없음). PG 확정과 가격 결정은 사용자 판단 대기(§B·§F). 구현 착수는 별도 승인 후 buildflow.
